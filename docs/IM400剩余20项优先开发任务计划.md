# IM400 剩余 20 项优先开发任务计划

更新时间：2026-07-17 22:36
当前基线：历史生产严格口径为已关闭 32 项、待闭环 36 项。两台 Huawei 已使用当前源码本地 Release 连接本机 Docker，原先仅有 API 或间接真机证据的消息 6 项、群资料 7 项、通话 9 项现已全部补齐原 UI 双真机严格证据，本地原 UI 待闭环项由 22 项降为 0。该结果证明当前源码在本地双机环境可用，但生产仍是旧后端，故继续将“本地 Docker PASS”和“生产原场景 PASS”分层记录；IM-001 仍缺真实短信网关和获批手机号。

## 完成口径

单个用例只有同时满足以下条件才标记为 `PASS`：

1. 客户端、服务端或原生层修复代码已落地；
2. 相关单元测试、静态检查和构建通过；
3. 按原失败场景所需设备数在华为真机复测通过；跨账号、实时同步、通话等场景必须两台，单机系统权限/渠道/网络状态不得因另一台离线被错误阻塞；
4. `FATAL EXCEPTION`、ANR、崩溃数为 0；
5. 截图、UI XML、logcat 和结构化结果已写入 `artifacts/p2-fix-validation/`；
6. 总修复方案和剩余数量已同步更新。

## 执行顺序

| 批次 | 范围 | 数量 | 依赖 | 当前状态 |
|---|---|---:|---|---|
| A | 消息可靠性：IM-210、IM-215、IM-205 | 3 | 无；IM-205 为 IM-357 的基础 | IM-210、IM-215 本地 Docker 双真机原 UI PASS；生产待部署复验 |
| B | 转发和收藏：IM-199、IM-216、IM-219、IM-220 | 4 | 复用 IM-215 的稳定转发队列 | 4 项本地 Docker 双真机原 UI PASS；生产待部署复验 |
| C | 群权限安全：IM-259、IM-260、IM-270、IM-271、IM-286 | 5 | IM-259 → IM-260；IM-270 → IM-271 | 本地 Docker 双真机实时场景 5/5 通过；待生产部署后复验 |
| D | 通话稳定性：IM-318、IM-323、IM-324、IM-339 | 4 | IM-323 的重连状态机 → IM-324 的超时收口 | IM-318、IM-323、IM-324 已在本地 Docker 双真机原 UI 通过；IM-339 已有真机严格证据，生产待部署复验 |
| E | 通知隐私：IM-345、IM-346、IM-350、IM-357 | 4 | 复用 IM-205 的撤回通知取消协议 | IM-346 本地 Docker 真机关闭/恢复预览通过；生产仍待新后端 |
| F | 后续通知与系统事件：IM-347、IM-349、IM-351、IM-355、IM-366、IM-379、IM-380 | 7 | 通知渠道、后台限制和连接状态 | IM-347 后台通知/渠道/点击与 IM-349 总开关 off/on 已在本地 Docker 真机通过；强停公告受 Huawei 系统限制单列，生产仍待部署复验 |
| G | 群资料与群管理：IM-249、IM-261、IM-262、IM-266、IM-267、IM-268、IM-269、IM-280、IM-285、IM-292、IM-293、IM-298 | 12 | 群资料协议、系统审计、公告回执、消息定位 | 群实时 4 项和群资料边界 7 项均已补本地双真机原 UI 证据；IM-298 已有真机消息定位证据，生产待部署复验 |

## 批次 A：消息可靠性

| 用例 | 修复任务 | 自动化门禁 | 双机验收 |
|---|---|---|---|
| IM-210 | 清空记录时同步清除消息窗口、会话摘要、时间、媒体预览与未读数；服务端保存 `cleared_at` 和空摘要并清理列表缓存 | Go 测试覆盖完整摘要清零；Flutter 测试覆盖本地摘要清空；刷新列表不恢复旧值 | A 仅为自己清空；A 聊天页及列表为空，刷新/重启不回弹；B 与其他会话不受影响 |
| IM-215 | 按服务端 `seq` 升序构建转发队列；缺少 `seq` 且时间相同时保持屏幕原顺序；批次不依赖页面存活，失败项使用同一幂等 ID 重试一次 | 覆盖乱序选择、同时间稳定顺序、临时失败重试、永久失败继续后续项 | 选择两条以上消息逐条转发，接收端顺序、数量一致且无重复 |
| IM-205 | 撤回事件按会话稳定通知 ID 取消 Android 原生/Flutter 本地通知；撤回不是最后一条消息时也必须清通知 | MethodChannel 和稳定 ID 测试；撤回分支不能因摘要序号提前返回 | B 不打开会话，A 发送唯一正文并撤回；B 通知栏和锁屏不再保留原文 |

## 批次 B：转发和收藏

| 用例 | 修复任务 | 验收重点 |
|---|---|---|
| IM-199 | 增加逐条/合并转发选择；服务端保存不可篡改快照；客户端支持嵌套记录预览 | 类型、发送者、时间、顺序正确，嵌套可打开 |
| IM-216 | 转发目标支持多选、计数和统一确认；按目标返回独立结果并增加幂等键 | 多会话各收到一次，部分失败可单独重试，重复点击不重复发送 |
| IM-219 | 收藏由本机 `SharedPreferences` 迁移为服务端模型/API/增量同步，本地仅作账号隔离缓存 | 同账号双机同步取消收藏，原消息保留，重装后状态正确 |
| IM-220 | 新增消息详情接口与页面，按私聊/群聊和角色展示创建、送达、已读信息 | 数据准确；普通成员看不到无权限的成员隐私 |

## 批次 C：群权限安全

| 用例 | 修复任务 | 验收重点 |
|---|---|---|
| IM-259 | 解散操作事务化，广播群终态；在线端关闭输入并退出旧页，离线端补拉终态 | 全员失去发送权限并收到明确状态 |
| IM-260 | 所有发送入口检查群终态，服务端保留最终校验；历史/通知深链进入只读页 | 无幽灵消息、无越权写入、提示准确 |
| IM-270 | 管理员任命后广播角色变化并使权限缓存失效 | 权限无需重启即时生效 |
| IM-271 | 撤销管理员后即时关闭入口；已打开表单提交仍由服务端拒绝 | 旧页面不能继续越权 |
| IM-286 | 服务端校验 `mention_all`、成员范围和频率，禁止普通成员伪造全员 UUID | 构造请求也被拒绝且不产生通知 |

## 批次 D：通话稳定性

| 用例 | 修复任务 | 验收重点 |
|---|---|---|
| IM-318 | 对同一双方同时呼叫做原子仲裁，稳定保留一个 `call_id` | 双端进入同一通话或得到明确忙线，不产生双通话 |
| IM-323 | 信令/RTC 断开显示重连状态和倒计时，短时恢复后继续原通话 | 通话计时不重置、媒体恢复、状态可见 |
| IM-324 | 超过阈值后服务端和客户端幂等结束并释放占用 | 双端结束原因一致，可立即重拨 |
| IM-339 | 发起和接听前检查麦克风/相机权限，提供降级与系统设置入口 | 首次拒绝、永久拒绝、仅相机拒绝三种路径正确 |

## 批次 E：通知隐私

| 用例 | 修复任务 | 验收重点 |
|---|---|---|
| IM-345 | 结合 App 生命周期、屏幕和当前会话决定是否抑制通知，锁屏不得只因 `activeChatId` 跳过通知 | 锁屏通知的声音、振动、亮屏符合设置 |
| IM-346 | 关闭预览时，本地与厂商推送统一使用通用标题和正文 | 锁屏/后台不泄露发送者和正文 |
| IM-350 | 读取 Android 实际通知权限和系统总开关，返回页面时刷新状态并提供设置入口 | 拒绝与重新授权后的 UI 状态实时准确 |
| IM-357 | 扩展 IM-205 的取消协议到旧通知点击和厂商离线推送 | 撤回后无原文残留，点击旧通知进入正确撤回状态 |

## 每批固定开发流程

1. 复现原失败步骤并锁定数据/状态边界；
2. 先补回归测试，再进行最小范围实现；
3. 运行相关 Go/Flutter 测试和 `flutter analyze`；
4. 生成 APK，并在两台真机安装同一构建；
5. 按原报告步骤复测，采集通知栏、锁屏、UI XML、logcat 与截图；
6. 只把证据完整的用例改为 `PASS`，随后更新剩余数量。

## 当前进度

- [x] 已确认 20 项范围、依赖和验收门禁。
- [ ] 批次 A：三项代码及定向测试通过，最终 APK 已双机安装。IM-215 在发送端真实目标会话验证两条按 C→D 完整落库且快速返回不再中断；旧线上后端未把转发实时/离线同步给接收端，因此 IM-210、IM-215、IM-205 均待部署新后端后完成双机闭环。
- [ ] 批次 B：IM-199、IM-216、IM-219、IM-220 的客户端/服务端代码、定向自动化与无需新接口即可验证的双机 UI 阶段均完成；线上仍为旧后端，合并快照、收藏同步、详情权威回执和转发状态同步必须部署后统一闭环，当前不新增 PASS。
- [ ] 批次 C：IM-259、IM-260、IM-270、IM-271、IM-286 的客户端/服务端代码与定向自动化完成；同版 APK 已成功安装两台真机，IM-286 已在群主真机验证“全体成员/管理员可用”和插入文本。发送端随后 ADB 离线，且线上仍为旧后端，终态广播、角色即时变更、伪造请求拒绝和双机通知尚待部署后闭环，当前不新增 PASS。
- [ ] 批次 D：IM-318、IM-323、IM-324、IM-339 的客户端/服务端代码与定向自动化完成；正式 Release 已在两台真机完成 Alice 发起、Bob 来电、Bob 拒接和双端无崩溃验证。并发同呼、断网重连和双端超时释放仍需按原场景专项闭环，当前不新增 PASS。
- [ ] 批次 E：IM-350 已真实关闭/恢复华为系统通知总开关；IM-345 已在活动会话锁屏、设备保持休眠时确认消息通知真实出现，两项严格关闭。IM-346、IM-357 仍待生产新后端协议闭环。
- [ ] 批次 F：IM-351、IM-355、IM-366、IM-379、IM-380 已严格关闭。正式 Release 的前台消息预览、后台系统通知和进程终止后通知均已双机通过；IM-347 公告分类和 IM-349 HMS 总开关仍需各自专项闭环。
- [ ] 批次 G：12 项群资料/群管理代码路径已完成。IM-298 已在最终 APK 搜索 `SEARCH_TARGET_015904`，准确定位目标并显示 4 条上下文，严格关闭；第二台真机已恢复，其余 11 项仍按各自群管理原场景逐项闭环。

### 2026-07-17 正式 Release 双真机回归

- 正式 APK：`artifacts/online-android-apk-20260717-124919/genericim-online-release.apk`；SHA256 `03FA5AC35570E8CC08E96CAFCDE5712C8427BBB2D4ACD98950C3F3B26E4778C4`。
- 两台 Huawei ELS-AN00 均安装 `5.0.0 (30)`；覆盖安装后两台 `base.apk` 哈希一致，正式包启动日志不再出现 Dart VM Service。
- 生产账号 `smoke_alice` 与 `smoke_bob` 分别在两台真机完成 UI 登录和单向发送，线上消息列表反查通过。
- 双机并发唯一消息 `A2B141237` / `B2A141237` 在两端各出现一次，顺序一致；线上后端会话 `275e597e-e4d7-48d5-89a5-ee81999f869b` 落库序号为 106/107。
- 接收端停留消息列表、退到桌面、被系统终止进程三种状态均收到对应消息预览或系统通知。
- Alice 发起真实语音呼叫，Bob 出现来电页并拒接；两端通话阶段 `FATAL/ANR/E/flutter=0`。
- 双机私聊 Logcat：`Crash=0`、`ANR=0`、`FATAL=0`、应用内 Socket 断连 `0`。华为 AGC/GMS/FCM 缺失和可选资源 404 仍记为非致命环境告警。
- 证据目录：`artifacts/real-device-qa/release-cross-account-20260717-1401/`。
- 本轮修复 QA 导航器：底部“消息”存在未读角标时，改为限定屏幕底部区域的包含匹配；正式通知用例重跑通过。
- 本轮只确认基础消息、通知和呼叫主链路，不改变剩余 39 项计数；群管理、弱网重连、并发同呼、公告分类等仍按原失败场景逐项验收。
- [ ] 20 项汇总回归和文档收口。

### 2026-07-16 批次 A 开发记录

- Go `internal/services`、`internal/handlers`：通过；
- Flutter 定向测试：19/19 通过；
- Flutter 目标静态检查：0 问题；
- Android Release Kotlin、Java 编译：通过；
- 两台 Huawei ELS-AN00 已安装最终生产接口 APK `local-real-device-apk-20260716-214602`，安装成功；
- IM-205 已在旧线上协议下复现：HMS 使用随机通知 ID，撤回后旧通知保留；据此补齐服务端稳定 ID 与 `message_revoked` 数据推送；
- IM-215 第一次真机操作暴露“关闭页面导致批次静默中断”，现已改为脱离页面生命周期继续、同幂等 ID 重试和明确失败计数；第二次选择 D 后 C 并立即返回，发送端目标会话完整显示 C→D；
- IM-215 双机在线/离线接收测试均显示旧线上后端未向接收端同步转发记录，发送端目标会话和列表已写入成功，需用新后端再次闭环；
- 已据双机结果补齐转发成功后的 `user_chat_state_changed` 广播，使接收端会话摘要、未读游标和增量窗口使用与普通发送相同的刷新协议；
- 双机 Fatal/ANR/Flutter 异常匹配：0；
- 当前不新增 PASS，基线仍为已修复 21 项、剩余 47 项：新后端尚未部署，三项都不能用旧线上服务伪造双机闭环。

### 2026-07-16 批次 B 开发记录

- IM-216 多目标选择器支持复选目标、实时已选计数和统一确认；
- 目标 ID 去重并保持选择顺序，每个目标独立执行稳定转发队列，一个目标失败不阻断其他目标；
- 群组/频道在提交前逐目标检查发言权限，受限目标不产生请求；
- 自动化新增多目标去重、失败隔离和幂等重试覆盖，消息可靠性测试 15/15 通过，目标静态检查 0 问题；
- 真机 APK：`artifacts/local-real-device-apk-20260716-221018/genericim-local-real-device-release.apk`，SHA256 `CBB13AF7243152A203DE5B7F17CFD4EA0D6BFC4EBE8AC852615D62B18B072786`；
- 真机选择 `smoke_bob` 与 `用户DC8333` 两个目标，两个目标内部均为 C→D，无重复，Fatal/ANR/Flutter 异常为 0；
- 旧线上后端仍未把目标 `smoke_bob` 的转发同步到接收端设备，因此 IM-216 暂不记 PASS。
- IM-219 新增 `message_favorites` 服务端模型，唯一键为用户、会话和消息；收藏内容由服务端读取 Mongo 原消息生成不可伪造快照；
- IM-219 新增收藏、取消、分页和增量同步接口，取消使用显式 tombstone，原聊天消息不执行删除或修改；
- 收藏变化通过 `message_favorite_changed` 发给同账号设备；Flutter 仅在远端操作成功后更新本地账号隔离缓存，打开收藏页用服务端结果覆盖缓存；
- IM-219 Go handlers/models/services/server 测试通过，Flutter 收藏隔离与可靠性合计 17 项通过，目标静态检查 0 问题；待部署后做同账号双机收藏/取消/重启验证。
- IM-220 新增权威消息详情接口：私聊返回唯一接收方送达/已读，群聊和频道按 `last_read_seq` 返回准确已读汇总；普通成员只看汇总，管理员可看成员明细；群聊无逐成员送达游标时明确标记不可精确统计，不伪造人数；
- IM-220 Flutter 新增长按“消息详情”和详情页；两台真机均验证入口、页面和旧接口不可用时的本机信息降级提示，Fatal/ANR/`E/flutter` 为 0；
- IM-199 多选后新增“逐条转发/合并转发”；合并模式由服务端重新读取源消息并按权威 `seq` 排序，保存发送者、类型、内容和时间的不可变快照；限制 2–100 条、去重、最多 3 层嵌套；
- IM-199 客户端新增聊天记录气泡、类型预览与嵌套记录逐层打开；每目标使用稳定幂等 ID 重试且目标去重、失败隔离；两台真机均显示两种模式入口，Fatal/ANR/`E/flutter` 为 0；
- 批次 B 最新 APK：`artifacts/local-real-device-apk-20260716-230300/genericim-local-real-device-release.apk`，SHA256 `5FF5A595CF9E7DBBA2C38E4ED3C352C03A7E955720E7CDCF9776087C7DC37FFD`；两台 Huawei ELS-AN00 安装成功；
- 最新 Go handlers/services/models/server 全通过，Flutter 合并快照、可靠性、详情、收藏隔离合计 21 项通过，`flutter analyze` 0 问题；
- 当前仍维持已修复 21 项、剩余 47 项：批次 B 的服务端新协议尚未部署，不能用 UI 阶段结果冒充完整双机 PASS。

### 2026-07-16 批次 C 开发记录

- IM-259：用户端和管理后台的解散路径统一为事务行锁 + `status=dissolved` 终态，不再硬删成员、`user_chats` 或历史；广播 `chat_dissolved`，清理会话列表和消息窗口缓存；在线聊天页停止输入/录音并退出，离线重连刷新终态；
- IM-260：普通发送、转发、合并转发、编辑、撤回、表情、置顶、红包、转账、群会议等写入口增加终态保护；消息和会议最终写入与解散锁同一会话行，避免状态检查后仍写入幽灵消息；已解散历史页显示只读提示；
- IM-270/IM-271：App 与管理后台任命/撤销管理员均在事务中复检会话终态，撤销时删除细分权限；广播 `member_role_changed` 并失效列表、详情和成员缓存，旧表单提交仍由服务端拒绝；
- IM-286：服务端拒绝非成员、保留字和超数量提及；普通成员不能伪造 `mention_all`，仅管理员/群主可用，Redis 按群和发送人限制每分钟一次；客户端只把 `mention_all` 作为独立协议字段，不把 `__all__` 伪装成成员 UUID；接收端列表识别全员提及；
- Go `handlers/services/models/cmd/server` 全通过；Flutter 提及协议、消息可靠性、合并快照、消息详情合计 21 项通过；目标 `flutter analyze` 0 问题；
- 同版 APK：`artifacts/local-real-device-apk-20260716-233012/genericim-local-real-device-release.apk`，SHA256 `4CB761DB273078A7EE116C07BBAFBAF3E17EBD0DC6E5B66B775DAAE53B9C1A75`；两台 Huawei ELS-AN00 均安装成功；
- 接收端/群主 `8MY0220C17006781` 新建双成员测试群，输入 `@` 后真实显示“全体成员 / 管理员可用”，选择后输入框为 `@全体成员`；证据为 `batch-c/8MY0220C17006781/im286-mention-all.png` 与 `im286-mention-all-selected.png`；Fatal/ANR/`E/flutter` 匹配为 0；
- 发送端 `UQG5T20915006269` 在安装成功并启动后变为 ADB `offline`，未伪造双机证据；线上旧后端也没有本批终态和提及协议。当前严格基线仍为已修复 21 项、剩余 47 项。

### 2026-07-17 批次 D 开发记录

- IM-318：服务端按双方用户 ID 固定顺序加行锁；反向同时呼叫不再取消第一条后创建第二个 ID，而是把第二个请求原子仲裁为对第一条的接听，复用原 `call_id`、频道和 RTC 凭证；客户端识别 `arbitrated` 并直接进入同一通话；
- IM-323：Agora/LiveKit 断开不再立即挂断，进入 `reconnecting` 状态并显示 20 秒倒计时；恢复后沿用原 `connectTime`、计时器和 `call_id`，不会重置通话时长；重连期间继续尝试心跳；
- IM-324：重连倒计时耗尽后客户端只执行一次 `reconnect_timeout` 结束；服务端 `EndCall` 已按行锁和终态幂等返回，后台清理服务继续以心跳超时兜底并向双端广播统一 `heartbeat_timeout` 释放状态；
- IM-339：发起和接听前先请求麦克风/相机；麦克风拒绝明确阻断，相机拒绝可改用语音，来电视频接听可自动降级为语音；永久拒绝和权限检查异常提供“前往设置”入口，不再静默自动跳转；
- Go `handlers/services/models/cmd/server` 全通过；Flutter 通话策略与既有定向套件合计 23 项通过；全仓 `flutter analyze` 0 问题；
- 最新 APK：`artifacts/local-real-device-apk-20260717-001559/genericim-local-real-device-release.apk`，SHA256 `2D184295968F27A29BF2D67CD55A2885BC5E5DCA253FF3D5B57721D1828D7AE3`；
- `8MY0220C17006781` 在最新版 APK 上撤销相机权限后发起视频，真实显示“通话权限未开启 / 改用语音 / 前往设置”；证据为 `batch-d/8MY0220C17006781/im339-final-same-build.png`，并已实测打开系统应用信息页；Fatal/ANR/`E/flutter` 匹配为 0；
- `UQG5T20915006269` 仍为 ADB `offline`，且生产后端未部署本批代码，IM-318/323/324 无法以单机或旧协议冒充双机通过。严格基线保持已修复 21 项、剩余 47 项。

### 2026-07-17 批次 E 开发记录

- IM-345：会话仅在 App `resumed` 时视为正在查看；Android 原生层只在 Activity 前台、屏幕可交互、键盘未锁且当前会话匹配时抑制通知。真机证据显示发送前后 `mWakefulness=Asleep`，同时消息通知数为 1，锁屏不再被残留 `activeChatId` 误抑制；
- IM-346：本地通知关闭预览后统一为“新消息 / 您收到一条新消息”；服务端同时隐藏发送者标题、正文和图片 URL，FCM/HMS 共用该数据。真机已验证开关关闭和通用正文；通知栏仍显示发送者标题，证实生产后端仍是旧代码，待部署复测；
- IM-350：MethodChannel 现同时读取 Android 13+ `POST_NOTIFICATIONS` 和 `NotificationManagerCompat.areNotificationsEnabled()` 系统总开关；页面返回前台立即刷新。真机从“已允许”变为“已关闭，点击去系统设置开启”，点击后实际进入华为应用通知页；
- IM-357：新消息推送增加 `msg_id/message_id`；撤回同时取消稳定 ID、扫描清理旧随机 ID，并保存 24 小时会话级/消息级墓碑。HMS 图片下载期间撤回也不再补显过期通知。真机撤回前消息通知数 1、撤回后 0；模拟旧 PendingIntent 点击时 logcat 记录 `revoked notification tap ignored`，页面未打开已撤回正文；
- Go 全仓测试通过，Flutter 定向通知测试 5/5 通过，`flutter analyze` 0 问题，Android Release APK 构建通过；
- 最新 APK：`artifacts/local-real-device-apk-20260717-003147/genericim-local-real-device-release.apk`，SHA256 `5DC540F74B9D65B67A166B37912F5BBF3D24C704405CC5FD07433FFF7FA7AABE`；在线 Huawei ELS-AN00 安装成功，Fatal/ANR/`E/flutter` 匹配为 0；
- `UQG5T20915006269` 仍为 ADB `offline`，生产后端仍为旧通知标题协议。严格基线保持已修复 21 项、剩余 47 项，不把单机和未部署结果提前记为 PASS。

### 2026-07-17 批次 F 开发记录

- IM-349 新增账号级通知总开关，本地显示/声音/振动与服务端推送策略统一受控；真机验证关闭和重新开启，分类选择未丢失；
- IM-351 为消息、公告、会议、后台服务建立独立 Android 通知渠道，真机 `dumpsys notification` 确认四个渠道存在；
- IM-355 增加通知栏 `RemoteInput` 回复。真机发现广播接收器拉起页面被华为后台启动策略拒绝，随后改为通知动作直接使用 Activity `PendingIntent`；输入 `QA_REPLY_013211` 后，对端服务端列表确认收到；
- IM-366 断网持续 2 秒显示“网络不可用/重试”，Wi-Fi 恢复后横幅自动消失；
- IM-379 设置页显示原生检测到的后台/电池限制；IM-380 偏移变化单测通过，但非 root 真机拒绝修改 `persist.sys.timezone`，时区保持 `Asia/Shanghai`；
- Go 全量、Flutter analyze、Kotlin Release 编译及 3 项聚焦测试全部通过；最新 APK 为 `artifacts/local-real-device-apk-20260717-012743/genericim-local-real-device-release.apk`，SHA256 `9807CD85660FE8EE42CB558D7AF62DFF7C4649D32F3C2E73BE5E070BFE480FCD`；
- Fatal/ANR/`E/flutter` 匹配为 0。生产后端未部署且另一台真机离线，严格基线仍为已修复 21 项、剩余 47 项。

### 2026-07-17 批次 G 开发记录

- 群名最多 32 字符，描述最多 1000 字符；两者统一去空格和敏感词策略，纯空格描述明确拒绝；
- 群昵称支持本人修改/清除及管理员按角色边界修改，成员列表优先显示群昵称并保留全局昵称；
- 群改名、管理员变更、群主转让、全员禁言、退出和群昵称修改补齐系统审计；群资料广播包含名称、描述、头像、群主和更新时间；
- 公告新增幂等确认回执表、成员鉴权、确认人数和客户端确认按钮；真机由 `1/2` 变为 `2/2`；
- 搜索结果补充消息 `seq`，搜索跳转按目标窗口装载且规避初始化竞争；真机目标 `SEARCH_TARGET_015904` 准确落点；
- 管理员撤回他人消息的分类优先于本机发出属性，并新增原发送者视角回归测试；
- 最终同源码 APK：`artifacts/local-real-device-apk-20260717-022315/genericim-local-real-device-release.apk`，SHA256 `3F7EA9B1C0CDB1BE5975E443E3567CB8CBCF0486BBE56D53B7575322C1FA7CDB`；覆盖安装后登录态与群列表保留；
- 在线真机 Fatal/ANR/`E/flutter` 匹配为 0；第二台执行重连后仍为 `offline`。严格基线仍为已修复 21 项、剩余 47 项。

### 2026-07-17 批次 H 与注册收口开发记录

- IM-305：前后端响铃超时统一为 30 秒，来电协议增加 `expires_at`，拒绝迟到的过期来电；在线真机呼叫离线 QA 联系人后约 30 秒自动返回。
- IM-308/337：新增视频降级语音、摄像头开关同步协议和对端摄像头关闭占位；后端禁止原通话从语音反向升级视频。
- IM-313/314/315：通话计时改为服务端连接时间的绝对差；锁屏、后台和页面恢复重新同步时长；视频后台自动停摄像头，回前台按原状态恢复。
- IM-332：视频通话开放横竖屏，真机呼叫页已实际横屏显示；语音和退出页面恢复竖屏。
- IM-001：复用现有短信网关完成注册验证码发送、独立缓存、频控、错误次数限制、手机号唯一绑定、事务成功后消费及 Flutter 注册页字段。
- 本地 Docker API 已从当前源码重建并健康；双账号 API 已验证创建过期时间、接听连接时间、摄像头关闭、视频降级语音和清理。
- Go 全量测试、Flutter analyze、定向测试和 Android release 构建通过。最终 APK 为 `artifacts/local-real-device-apk-20260717-024541/genericim-local-real-device-release.apk`，SHA256 `EFDCF9A8629BBECA85E385E41D7F02CB7BF34189E8198532BBD5FB0C759B9346`；在线真机覆盖安装与启动通过，Fatal/ANR/`E/flutter` 为 0。
- 第二台真机仍为 ADB `offline`，短信网关没有获批真实测试号码/发送凭据，生产后端也未获部署授权，因此严格基线仍为已修复 21 项、剩余 47 项，不把代码完成和单机证据冒充双机全量 PASS。

### 2026-07-17 剩余项持续回归记录

- 后端 `go test ./...` 全仓通过；Flutter `analyze` 0 问题，Flutter 全量测试 137 项通过；管理后台 `pnpm.cmd build` 通过，构建内置的 `vue-tsc --noEmit` 同时通过。
- 新增可重复执行的 `scripts/real-device-qa/Run-IM400-RemainingSingleDevice.ps1`，用于核验真机实际 APK、冷启动、前后台恢复、截图/UI XML 和 Logcat 错误计数，不清理登录态。
- 在线 Huawei `8MY0220C17006781` 回读的安装包与最终 APK SHA256 完全一致：`EFDCF9A8629BBECA85E385E41D7F02CB7BF34189E8198532BBD5FB0C759B9346`。
- 冷启动与后台 8 秒恢复的进程 PID 均为 `31102`，页面和登录态正常；`FATAL EXCEPTION`、应用 ANR、`E/flutter`、`FlutterError`、`DioException`、`SocketException` 均为 0。
- 本轮证据位于 `artifacts/real-device-qa/im400-remaining-single-device/20260717-025537/`；未发现新的代码失败，因此继续复用已核验的最终 APK，不制造无源码差异的新构建。
- 第二台 Huawei 经离线重连和重启 ADB 服务后仍为 `offline`；生产后端未获部署授权，短信到达测试也缺真实网关/号码。因此严格基线仍为已修复 21 项、剩余 47 项。

### 2026-07-17 Batch I 本地协议与单真机回归

- 新增 `scripts/real-device-qa/run_im400_remaining_api.py`，使用三个临时账号在当前本地 API 上验证消息、群权限与通话权威状态；访问令牌不写入报告。
- IM-210、IM-215、IM-216、IM-219、IM-220、IM-259、IM-260、IM-270、IM-271、IM-286、IM-305、IM-308、IM-318、IM-324、IM-337 共 15 项协议断言通过。
- 覆盖双向清空隔离、转发 `seq` 和幂等、收藏 tombstone、消息详情非成员 403、解散终态、管理员即时任命/撤销、`@全体` 权限、30 秒到期、反向同时呼叫仲裁、媒体越权、视频降语音及通话幂等释放。
- Go 全仓通过，Flutter Analyze 0 问题，消息/提及/通话定向测试 22 项通过，Python 脚本编译通过。
- 在线真机最终 APK 哈希一致；冷启动和后台恢复 PID 均为 `32212`，Fatal/ANR/Flutter 异常为 0。证据位于 `artifacts/real-device-qa/im400-remaining-single-device-batch-i/20260717-030917/`。
- 详细报告：`artifacts/p2-fix-validation/real-device-20260716-2035/batch-i/BATCH_I_DEVELOPMENT_REPORT.md`。
- 本地协议通过仍不能替代生产部署和同版本双真机实时事件/UI 验收；第二台设备仍 `offline`，严格基线保持已关闭 21 项、待完整验收 47 项。

### 2026-07-17 Batch J 撤回一致性与群管理协议回归

- 首轮回归发现并修复 IM-205：撤回已更新历史与摘要，但接收方未读数没有回退。服务端现按原发送者、成员已读游标、撤回 `seq` 和当前未读数精确扣减一次，并失效消息窗口/会话列表缓存、广播权威会话状态。
- 新增撤回未读判定单测，覆盖接收方、发送者、已经读过、零未读和无效序号；修复后 API 确认未读数精确减 1，历史和摘要同步一致。
- 新增 `scripts/real-device-qa/run_im400_group_message_api.py`，本地当前源码 API 的 14 组断言全部通过，覆盖 IM-199、IM-205、IM-249、IM-261、IM-262、IM-266、IM-267、IM-268、IM-269、IM-280、IM-285、IM-292、IM-293、IM-298。
- Go 全仓通过；Flutter Analyze 0 问题；消息可靠性、群协议、撤回、提及和详情定向测试 28/28 通过；本地 Docker API 已从当前源码重建并健康。
- 在线 Huawei `8MY0220C17006781` 安装包哈希与最终 APK 一致；冷启动和后台恢复 PID 均为 `1904`，Fatal/ANR/Flutter 异常为 0。证据位于 `artifacts/real-device-qa/im400-remaining-single-device-batch-j/20260717-032433/`。
- 详细报告：`artifacts/p2-fix-validation/real-device-20260716-2035/batch-j/BATCH_J_DEVELOPMENT_REPORT.md`。
- 第二台 Huawei 重连后仍不可用，生产后端未获部署授权；严格基线保持已关闭 21 项、待完整验收 47 项，不把本地协议和单机真机结果提前标为双机 PASS。

### 2026-07-17 Batch K 通话权威时间与通知设置回归

- 首轮当前源码 API 回归复现 IM-346/IM-349 的真实缺陷：新账号第一次提交 `enabled=false`、`show_preview=false` 后，GORM 模型的 `default:1` 把显式 `false` 覆盖为 `true`。
- 服务端已改为基于 `user_id` 的 map 原子 upsert，首次写入保留 `false`，部分更新互不覆盖；更新后回读并返回权威值，数据库异常不再静默当成默认设置。
- 新增 Go 回归测试和 `scripts/real-device-qa/run_im400_call_notification_api.py`。本地当前源码 API 覆盖 IM-313、IM-314、IM-315、IM-323、IM-346、IM-349 共 6 项并全部通过。
- 通话双方共享同一连接时间；心跳、摄像头状态和活动快照不重置计时；结束时长和重复结束终态稳定；非参与者操作被拒绝。
- Go 全仓、Flutter 全量 137 项、Flutter Analyze、Python 编译和差异检查均通过；本地 Docker API 从当前源码重建并健康。
- 在线 Huawei `8MY0220C17006781` 的 APK 哈希一致，冷启动和后台恢复 PID 均为 `3955`，Fatal/ANR/Flutter 异常为 0。证据位于 `artifacts/real-device-qa/im400-remaining-single-device-batch-k/20260717-033631/`。
- 详细报告：`artifacts/p2-fix-validation/real-device-20260716-2035/batch-k/BATCH_K_DEVELOPMENT_REPORT.md`。
- 第二台 Huawei 仍为 ADB `offline`，生产后端未获部署授权；严格基线保持已关闭 21 项、待完整验收 47 项。

### 2026-07-17 Batch L 注册输入安全与新包真机回归

- 定向测试首先复现 IM-001 两个输入边界：后端会接受 `10000000000`、`12000000000` 等无效号段，6 字符非数字内容会通过注册码格式检查。
- 后端大陆手机号规则已收紧为 `1[3-9]` 开头的 11 位数字，注册短信码改为严格 6 位数字；发送注册码接口先校验输入，再检查短信网关可用性。
- Flutter 注册、找回密码、绑定手机号三个入口统一复用共享手机号校验，避免客户端规则互相漂移。
- 新增 `scripts/real-device-qa/run_im400_registration_api.py`，当前源码本地 API 的 4 组断言全部通过：无效号段发送/注册均为 400、`ABC123` 注册为 400、手机号可选注册保持成功。
- Go 全仓通过；Flutter 全量 139 项、Flutter Analyze、Python 编译和差异检查通过；本地 Docker API 已从当前源码重建并健康。
- 新 APK：`artifacts/local-real-device-apk-20260717-batch-l/genericim-batch-l-release.apk`，SHA256 `794DC65C059147EC10633B5063E4FCE0DB8FAD232DE69C18BD6B458E967FBC49`。
- 在线 Huawei `8MY0220C17006781` 覆盖安装成功且设备内 APK 哈希一致；冷启动和后台恢复 PID 均为 `6872`，Fatal/ANR/Flutter 异常为 0。证据位于 `artifacts/real-device-qa/im400-remaining-single-device-batch-l/20260717-035157/`。
- 真实短信到达仍缺已配置网关和获批号码，生产后端未获部署授权，第二台 Huawei 仍为 ADB `offline`；严格基线保持已关闭 21 项、待完整验收 47 项。

### 2026-07-17 Batch M 注册生命周期与跨模块回归

- 注册生命周期首轮发现 QA 脚本把用户名截到 20 位后丢失唯一后缀，会碰撞历史测试账号；现改为短场景前缀加随机 UUID，连续执行两轮均为 7/7 通过。
- 使用本地 Redis 合成验证码验证 IM-001：5 次错误被拒绝，第 6 次返回 429 并撤销验证码；正确码注册成功且立即消费；重放返回 400；重新验证后的重复手机号返回 409；过期码返回 400；手机号可选注册保持可用。
- 重新执行消息/群权限/通话通知三套当前源码 API：9+14+2 组断言全部通过，合计覆盖 35 个既有 IM 编号。
- 后端 Go 全仓、Flutter 全量 139 项、Python 编译和差异检查通过。
- 在线 Huawei `8MY0220C17006781` 的设备内 APK 与目标 SHA256 一致；冷启动和后台恢复 PID 均为 `7797`，Fatal/ANR/Flutter 异常为 0。证据位于 `artifacts/real-device-qa/im400-remaining-single-device-batch-m/20260717-035857/`。
- 详细报告：`artifacts/p2-fix-validation/real-device-20260716-2035/batch-m/BATCH_M_DEVELOPMENT_REPORT.md`。
- 第二台 Huawei 重连后仍为 `offline`，生产后端和真实短信前置条件不变；严格基线保持已关闭 21 项、待完整验收 47 项。

### 2026-07-17 Batch N 通知权限、渠道与系统状态严格收口

- IM-350：修复系统通知总开关关闭后启动页自动强制跳回系统设置的问题；真实关闭时 App 显示“已关闭”，恢复时立即显示“已允许”，系统状态已自动还原；
- IM-351：严格复核发现既有渠道缺少独立来电分类，已新增 `genericim_calls_v1` 并打通 HMS 与服务端厂商推送路由；最终真机确认消息、来电、公告、会议、后台服务 5 个渠道存在；
- IM-366：真实关闭 Wi-Fi 与移动数据后 8 秒内显示“网络不可用/连接中断”和“重试”，恢复网络后横幅自动消失；
- IM-379：当前 Huawei 的真实后台/电池限制被原生检测，App 明确显示“已检测到后台/电池限制，点击处理”和电池设置入口；
- 最终 APK：`artifacts/local-real-device-apk-20260717-batch-n/genericim-batch-n-release.apk`，SHA256 `44AB2AC90CF95413DE099D8E1DB2BB5EC3E5A2262BDC1315D607B741C049F3B5`；设备内哈希完全一致；
- Flutter 全量 139/139、Analyze 0 问题、Go 全仓通过；冷启动与后台恢复 PID 均为 `19719`，Fatal/ANR/FlutterError 为 0；
- 详细报告：`artifacts/p2-fix-validation/real-device-20260716-2035/batch-n/BATCH_N_DEVELOPMENT_REPORT.md`；
- 4 项原失败边界均为单设备本地系统状态，已按原步骤在当前 APK 真机闭环。本轮严格新增 4 项 PASS，基线更新为已关闭 25 项、待完整验收 43 项。

### 2026-07-17 Batch R 撤回推送兼容合同

- IM-357 撤回控制事件补齐 `msg_id/message_id` 双字段、`scene=message_revoked` 与服务通知分类；FCM/HMS 请求均保持数据消息，不额外展示撤回通知；
- 新增 `scripts/real-device-qa/run_im357_revoke_push_api.py`。隔离接收账号关闭通知总开关后，当前源码仍将撤回控制事件路由到 HMS，撤回者没有收到自己的事件；
- Go 全仓、当前源码本地 Docker API 和相邻群消息/撤回 API 14/14 组通过；
- 两台 Huawei 仍为 ADB `offline`，真实厂商到达和通知栏原文清除尚不能复测；IM-357 保持待真机闭环，严格基线仍为已关闭 29 项、剩余 39 项；
- 详细报告：`artifacts/p2-fix-validation/real-device-20260716-2035/batch-r/BATCH_R_DEVELOPMENT_REPORT.md`。

### 2026-07-17 Batch S 通知预览数据层隐私

- IM-346 新消息推送增加独立数据合同和 `scene=new_message`；预览关闭时四种媒体 URL 别名均不进入厂商推送数据；
- 隔离账号验证预览关闭时标题/正文通用化、发送者和唯一正文不泄露，重新开启预览后正确恢复；
- IM-349 总开关关闭后普通消息推送日志为 0；IM-357 撤回清除事件仍可进入 HMS，两条策略未互相误伤；
- Go 全仓和当前源码 Docker API 通过；两台 Huawei 仍为 ADB `offline`，严格基线保持已关闭 29 项、剩余 39 项；
- 详细报告：`artifacts/p2-fix-validation/real-device-20260716-2035/batch-s/BATCH_S_DEVELOPMENT_REPORT.md`。

### 2026-07-17 Batch T 同时呼叫与 RTC 频道唯一性

- IM-318 增加真正并发双向呼叫压力验证；首轮发现秒级频道名在快速重拨时重复，已改为参与者前缀加随机 UUID 后缀；
- 500 次频道生成单测零重复；修复后 50 轮并发为 50 个唯一通话、50 个唯一频道、0 双呼叫、0 残留占用；
- 通话计时、媒体状态和结束幂等相邻回归通过，当前源码 Docker API 为 `healthy`；
- 两台 Huawei 仍为 ADB `offline`，真实双机 RTC 入房和 UI 收敛待验证；严格基线仍为已关闭 29 项、剩余 39 项；
- 详细报告：`artifacts/p2-fix-validation/real-device-20260716-2035/batch-t/BATCH_T_DEVELOPMENT_REPORT.md`。

### 2026-07-17 Batch U HMS 过期来电通知收口

- IM-305 新增原生 HMS `expires_at` 校验，在首次接收和异步图片加载完成后分别拦截过期来电；
- 有效来电通知使用剩余 TTL 自动超时，支持 RFC3339、Unix 秒和 Unix 毫秒，同时保留旧协议兼容；
- Kotlin 4/4、Flutter 定向 4/4、Flutter 全量 140/140、Analyze、Go 全仓及 Android Release 构建全部通过；
- 新 APK SHA256 为 `486CA374B4B06E01A1E3F1D8623A71C3D1A949F7C9715B39B5BE614566E43581`；
- 两台 Huawei 均为 ADB `offline`，真实 HMS 延迟到达与通知自动消失待设备恢复后验证；严格基线仍为已关闭 29 项、剩余 39 项；
- 详细报告：`artifacts/p2-fix-validation/real-device-20260716-2035/batch-u/BATCH_U_DEVELOPMENT_REPORT.md`。

### 2026-07-17 Batch V 厂商来电推送 TTL 收口

- IM-305 的 FCM/HMS 来电供应商队列 TTL 已绑定 `expires_at`，最大 30 秒，防止失效呼叫继续在 24 小时队列中延迟投递；
- 旧协议安全上限、RFC3339、Unix 时间、过期和超长到期等 6 个策略边界及两种供应商请求均有 Go 测试；
- Go 全仓、Flutter 140/140、Analyze 及本地当前源码 Docker API 健康检查全部通过；
- 本批无移动端源码差异，复用 Batch U APK；两台 Huawei 均为 ADB `offline`，真实 HMS/FCM 延迟丢弃待设备恢复和获批环境验证；
- 严格基线仍为已关闭 29 项、剩余 39 项；详细报告：`artifacts/p2-fix-validation/real-device-20260716-2035/batch-v/BATCH_V_DEVELOPMENT_REPORT.md`。

### 2026-07-17 Batch W 群公告稳定通知身份

- IM-347 增加确定性公告 `notification_id`，与普通消息 ID 范围分离；FCM 使用稳定 tag，HMS 使用同一数据 ID；
- Android 点击字段和 Flutter 去重签名补齐 `announcement_id/notification_id`，同群不同公告不会被误合并；
- Go 全仓、Flutter 141/141、Analyze、Release APK 及本地当前源码 API 健康验证通过；
- 新 APK：`artifacts/local-real-device-apk-20260717-073748/genericim-local-real-device-release.apk`，SHA256 `C48BAD9C908E9584FBEF8313AD67AC000B0EE5A21D771357CE34CF1051E25F09`；
- 两台 Huawei 均为 ADB `offline`，真实厂商重复投递与冷/热启动点击待恢复后验证；严格基线仍为已关闭 29 项、剩余 39 项；
- 详细报告：`artifacts/p2-fix-validation/real-device-20260716-2035/batch-w/BATCH_W_DEVELOPMENT_REPORT.md`。

### 2026-07-17 Batch X FCM/HMS 撤回通知精确取消

- IM-357 的 FCM 新消息增加每会话稳定 tag，撤回按 SDK 的 `tag + id 0` 取消，同时保留应用数字 ID 清理；
- Android 前台、HMS 原生与 Flutter headless fallback 三条取消路径使用同一身份合同；
- Go、Kotlin、Flutter 定向测试、Go 全仓、Flutter 142/142、Analyze、Release APK 和本地 API 健康验证全部通过；
- 新 APK：`artifacts/local-real-device-apk-20260717-074704/genericim-local-real-device-release.apk`，SHA256 `3FD0E4F3D40EECE258BA42194BE3640F5C941C5FCFC98FD5EE027C176839951D`；
- 两台 Huawei 均为 ADB `offline`，真实锁屏撤回通知清除待恢复后验证；严格基线仍为已关闭 29 项、剩余 39 项；
- 详细报告：`artifacts/p2-fix-validation/real-device-20260716-2035/batch-x/BATCH_X_DEVELOPMENT_REPORT.md`。

### 2026-07-17 Batch Y FCM 过期来电客户端收口

- IM-305 的 FCM 后台 CallKit 增加到期拦截，迟到且已过期的来电不再展示；
- 前台/后台统一解析 RFC3339、Unix 秒、Unix 毫秒，并按服务端剩余时间缩短 CallKit 展示时长；
- Flutter 定向 8/8、全量 146/146、Analyze、Go 全仓、Release APK 和本地 API 健康验证通过；
- 新 APK：`artifacts/local-real-device-apk-20260717-075926/genericim-local-real-device-release.apk`，SHA256 `D672C06FE8A1D7599820C53ED086A3B12F169E4FA7AE56622B29F2C005E12063`；
- 两台 Huawei 均为 ADB `offline`，真实 FCM/HMS 延迟投递仍待设备恢复；严格基线仍为已关闭 29 项、剩余 39 项；
- 详细报告：`artifacts/p2-fix-validation/real-device-20260716-2035/batch-y/BATCH_Y_DEVELOPMENT_REPORT.md`。

### 2026-07-17 Batch Z FCM 总开关最终门控

- IM-349 的 FCM 普通消息从 `5.0.0+26` 起使用 data-only，由客户端后台 isolate 读取本机总开关后决定是否展示；
- 服务端按设备 AppVersion 保留旧客户端自动通知协议，新客户端先发布、后端后部署即可安全灰度；
- 本地通知点击补齐完整路由和冷启动恢复，账号退出时清空待处理点击；
- Flutter 149/149、Analyze、Go 全仓、Release APK 及本地 API 健康验证通过；
- 新 APK：`artifacts/local-real-device-apk-20260717-081007/genericim-local-real-device-release.apk`，SHA256 `629A93427ECE998E3C154D7D61F1EAFF13DC782FB93DEE544BF7D6783FA91CFF`；
- 两台 Huawei 均为 ADB `offline`，真实总开关 off/on 与排队投递仍待设备恢复；严格基线仍为已关闭 29 项、剩余 39 项；
- 详细报告：`artifacts/p2-fix-validation/real-device-20260716-2035/batch-z/BATCH_Z_DEVELOPMENT_REPORT.md`。

### 2026-07-17 Batch AA 快捷回复与公告门控补全

- 新版 FCM 本地消息通知补回 RemoteInput 快捷回复，继续复用 IM-355 的幂等发送与登录态恢复；
- `chat_announcement` 纳入 `5.0.0+26` 客户端总开关门控，旧客户端继续使用自动公告通知；
- 公告保持独立渠道、稳定通知 ID 和完整点击路由，且不会错误出现消息回复动作；
- Flutter 150/150、Analyze、Go 全仓、Release APK 及本地 API 健康验证通过；
- 新 APK：`artifacts/local-real-device-apk-20260717-081813/genericim-local-real-device-release.apk`，SHA256 `F24BEE2BF5AEAC58661BF55F0DFBA9312370C43A46E83486A605C279FE68DF8E`；
- 两台 Huawei 均为 ADB `offline`，真实快捷回复、公告 off/on、冷/热启动点击仍待恢复；严格基线仍为已关闭 29 项、剩余 39 项；
- 详细报告：`artifacts/p2-fix-validation/real-device-20260716-2035/batch-aa/BATCH_AA_DEVELOPMENT_REPORT.md`。

### 2026-07-17 Batch AB FCM 后台来电总开关门控

- 复查确认 HMS 来电会读取原生总开关，但 FCM data-only 来电后台 isolate 会直接打开 CallKit；现已统一为同一本机最终裁决；
- CallKit 展示前执行两次持久化总开关读取，同时保留 payload 合法性与 `expires_at` 到期拦截；
- Flutter 定向 17/17、全量 153/153、Analyze 0、Go 全仓和 Release APK 均通过；
- 新 APK：`artifacts/local-real-device-apk-20260717-083132/genericim-local-real-device-release.apk`，SHA256 `4EEB22C63CE08483ADD68129D43EC88E7FD0ADFA3C9AEF7AA846855280A0B584`；
- 两次重连及重启 ADB server 后两台 Huawei 仍为 `offline`，无法安装及采集 FCM 来电 off/on 证据；IM-349 继续待真机，严格基线保持已关闭 29 项、剩余 39 项；
- 详细报告：`artifacts/p2-fix-validation/real-device-20260716-2035/batch-ab/BATCH_AB_DEVELOPMENT_REPORT.md`。

### 2026-07-17 Batch AC FCM 会议通知总开关门控

- 会议邀请、入会申请、审核结果和会议状态从 `5.0.0+27` 起改为客户端 data-only 门控，阻止 FCM 自动通知绕过 App 总开关；
- `+26` 及旧客户端继续使用自动会议通知；服务端按业务类型分别保持消息/公告 `+26` 门槛和会议 `+27` 门槛；
- 四种会议事件增加稳定 ID、统一会议渠道和完整点击路由，补齐 `meeting_status` 点击无响应及 HMS 审核结果渠道遗漏；
- Flutter 153/153、Analyze 0、Go 全仓、Release APK 与当前源码 API 镜像健康验证通过；
- 新 APK：`artifacts/local-real-device-apk-20260717-084107/genericim-local-real-device-release.apk`，SHA256 `DA4C8995B3CDB8E15F2123637B89E97E146AF5844B56A148899B8B9DCE48F0E5`；
- 两台 Huawei 重连和 ADB server 重启后仍为 `offline`，无法安装及采集真实会议通知 off/on、替换与点击证据；严格基线保持已关闭 29 项、剩余 39 项；
- 详细报告：`artifacts/p2-fix-validation/real-device-20260716-2035/batch-ac/BATCH_AC_DEVELOPMENT_REPORT.md`。

### 2026-07-17 Batch AD 系统广播离线推送与总开关门控

- 管理后台系统广播在数据库持久化成功后新增离线供应商推送，普通用户即使不在线也能收到，WebSocket 与推送共用 `broadcast_id`；
- `system_announcement` 从客户端 `5.0.0+28` 起改为 data-only 本地门控，`+27` 及旧客户端继续自动通知，支持先客户端、后后端的安全灰度；
- 系统广播使用公告渠道、稳定 ID/tag，并补齐冷/热启动点击后的标题和正文展示；
- Flutter 153/153、Analyze 0、Go 全仓、Release APK 与当前源码 API 镜像健康验证通过；
- 新 APK：`artifacts/local-real-device-apk-20260717-085549/genericim-local-real-device-release.apk`，SHA256 `CA56524A33A9A01E2E11145DC915B8BFA77C4921B1570E2C890A11AAC32EE1BC`；
- ADB server 重启、离线重连和逐台重连后两台 Huawei 仍为 `offline`，无法安装及采集真实系统广播 off/on、稳定替换与点击证据；严格基线保持已关闭 29 项、剩余 39 项；
- 详细报告：`artifacts/p2-fix-validation/real-device-20260716-2035/batch-ad/BATCH_AD_DEVELOPMENT_REPORT.md`。

### 2026-07-17 Batch AE 两台真机恢复与剩余项终审

- 两台 Huawei 均恢复为 ADB `device`，正式 Release `5.0.0+30` 完成双账号、消息、通知和通话专项；IM-332、IM-339、IM-357 已按原场景严格关闭；
- 消息/转发、群权限/公告、通话通知三套当前源码 API 最终回归覆盖原剩余清单 34 项并全部通过；IM-318 的 10 轮并发为 10 个唯一通话、10 个唯一频道、0 双呼叫、0 残留占用；
- IM-001 生产 `sms_bind_ready=false`，真机注册页不展示手机号/验证码字段，缺真实网关和获批号码；本地输入与验证码合同 4/4 通过，但不记真实短信 PASS；
- IM-347 两轮生产专项均在 Bob 已登录条件下执行，后台和强停公告都没有新通知，Fatal/ANR/FlutterError 为 0；当前源码本地 `group_notice -> hms` 路由通过，结论指向生产后端/推送实现未部署；
- 修复 `p0_real_device_logout.ps1` 对被底栏遮挡语义节点和应用内网页的误导航，并新增 `run_im347_real_device.py` 作为可重复证据工具；
- 分层状态为：原 39 项全部已执行，3 项真机严格 PASS、34 项当前源码 PASS 待生产原场景、2 项生产/外部未通过。证据：`artifacts/real-device-qa/remaining-39-20260717-1430/`；本批未提交、未推送 Git。

### 2026-07-17 Batch AF 本地 Docker 双真机终验

- 两台 Huawei 均连接本机当前源码 Docker，使用本地 Release SHA256 `8643D76CFBAC7B2932F79DD022675932C6B8A9922C5CD7341E75B61B5A081447` 和本地账号完成测试，未调用生产 API；
- IM-346 通知隐私、IM-349 总开关 off/on、IM-347 后台公告渠道与点击均通过；强停后的公告未送达按 Huawei/Android 限制单列；
- 群管理实时 9 项、双机消息状态 6 组、真实语音 RTC、40 条突发消息与 10 轮通话生命周期通过，双端应用 Fatal/ANR/FlutterError 为 0；
- 修复已解散群仍占用 VIP 创建配额的问题，当前 Docker 重建后已由“解散旧群再创建”和群实时 r9 共同验证；
- 最终本地 API 回归覆盖消息/群、通话/通知、剩余边界和注册输入共 29 个断言组，全部 PASS；Go services/handlers、44 个 Python 脚本编译、4 个 PowerShell 套件解析及 `git diff --check` 通过；
- 仍需原 UI/外部条件闭环：IM-001 真实短信网关与获批手机号；IM-249、IM-262、IM-267、IM-268、IM-269、IM-292、IM-293、IM-305、IM-308、IM-313、IM-314、IM-315、IM-318、IM-323、IM-324、IM-337 当前主要是 API 或间接真机证据。生产未部署，本批不提交、不推送 Git。

### 2026-07-17 Batch AG 消息操作双真机严格闭环

- IM-199、IM-210、IM-215、IM-216、IM-219、IM-220 已在本地 Docker + 两台 Huawei 上按原 UI 场景 6/6 PASS；
- 覆盖多选逐条转发双目标、合并聊天记录快照与嵌套打开、权威消息详情及群成员隐私、同账号双机收藏删除同步、仅自己清空后重启不恢复且对端保留；
- 权威证据：`artifacts/real-device-qa/local-docker-dual-device-20260717/message-strict-ui-final/20260717-190359/case-result.json`；双端 Fatal/ANR/FlutterError 为 0；
- 新增可重复的双机 APK 准备和消息严格 UI 执行器，修复群聊会话名位于第三语义字段时通用导航无法打开的问题；
- 本地原 UI 待闭环项由 22 项降为 16 项，另有 IM-001 外部短信条件。本批不提交、不推送 Git。

### 2026-07-17 Batch AH 群资料边界双真机严格闭环

- IM-249、IM-262、IM-267、IM-268、IM-269、IM-292、IM-293 已在本地 Docker + 两台 Huawei 上按原 UI 场景 7/7 PASS；
- 移动端“编辑群组”补齐群名称和群简介输入框，名称限制 32 字符、简介限制 1000 字符并在提交前校验；无群组号时不再显示误导性的用户名规则说明；
- 覆盖群名称 32/33 字符、群简介 1000/1001 字符、群组号显示与无群组号状态、头像/资料编辑入口和实时同步；双端 Fatal/ANR/FlutterError 为 0；
- 权威结果：`artifacts/real-device-qa/local-docker-dual-device-20260717/group-boundary-strict-ui/20260717-211748/case-result.json` 与 `artifacts/real-device-qa/local-docker-dual-device-20260717/group-boundary-strict-ui/20260717-213222/case-result.json`；
- 本地原 UI 待闭环项由 16 项降为 9 项，另有 IM-001 外部短信条件。本批不提交、不推送 Git。

### 2026-07-17 Batch AI 通话生命周期与视频双真机严格闭环

- 最终 APK：`artifacts/local-real-device-apk-20260717-223424/genericim-local-real-device-release.apk`，SHA256 `E3136FE11CBB282FE7C25CF5AFE7BFBA68E2DA3B0BD07C87390D7B4E83A406C6`，已覆盖安装两台 Huawei；
- IM-313、IM-314、IM-323、IM-324、IM-305、IM-318、IM-315、IM-337、IM-308 已按原 UI 场景 9/9 PASS：锁屏/后台计时连续、短断网同 call_id 恢复、长断网超时释放与立即重拨、30 秒无人接听过期、同时呼叫单 call_id、视频后台恢复、摄像头关闭/恢复和视频降语音均通过；
- IM-318 API 真并发 10/10 仍保持 10 个唯一 call_id、0 双呼叫、0 残留占用；双真机可靠性套件全绿；
- 修复严格 UI 执行器的状态栏时间误判、LiveKit 断链等待窗口、重拨请求字段、同名摄像头语义节点误点和 Huawei/uiautomator2 短暂断连；产品通话代码未因自动化误判引入额外改动；
- 权威结果：`call-lifecycle-strict-ui/20260717-215633`、`20260717-220641`、`20260717-221202`、`20260717-221413`、`20260717-221945`、`20260717-222137`、`20260717-223455`；所有最终批次双端 Fatal/ANR/FlutterError 为 0；
- 本地当前源码原 UI 待闭环项由 9 项降为 0；仅 IM-001 继续受真实短信网关和获批手机号阻塞。生产未部署，本批不提交、不推送 Git。
