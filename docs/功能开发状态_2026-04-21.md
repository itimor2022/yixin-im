# 功能开发状态（2026-04-21）

## 2026-04-21 增量完善记录（持续）
| 项目 | 状态 | 说明 |
|---|---|---|
| 会议主持人转移后静音锁一致性 | 已完成 | 主持人转移时后端清理新主持人静音状态，前端在 `meeting_host_changed` 后同步解除本地锁。 |
| 会议加入人数上限校验 | 已完成 | `POST /meeting/join` 已增加上限校验，覆盖新加入与重入路径。 |
| 会议超员提示本地化 | 已完成 | 后端返回统一中文 `会议人数已满`，前端保留兼容映射。 |
| 踢出用户事件隔离 | 已完成 | 会议事件广播默认排除 `kicked` 成员，避免被移出后继续收到会中事件。 |

## 2026-04-21 新增功能排查修复（第二轮）
| 项目 | 状态 | 说明 |
|---|---|---|
| 会议加入并发超员风险 | 已完成 | `JoinMeeting` 增加事务内 `FOR UPDATE` 锁会议行，容量校验改为锁内校验，避免并发超员。 |
| 注销账号会议数据清理缺口 | 已完成 | 补齐 `meeting_invites / meeting_participants / meetings` 清理链路（含创建者会议、邀请关联、参会关联与邀约人引用归零）。 |
| 隐私设置首次更新默认值丢失 | 已完成 | `UpdatePrivacySettings` 在无记录时先按默认值初始化，再应用局部字段更新，避免未提交字段被置零值。 |
| WS 订阅/输入态越权风险 | 已完成 | `subscribe` 增加会话成员鉴权；`typing/read` 仅允许已订阅会话广播，阻断跨会话伪造。 |
| 前端 WS 连接状态访问告警 | 已完成 | `WebSocketService` 新增 `connectionState` getter，替换 provider 内直接访问 `ws.state`。 |
| 验证结果 | 已完成 | 后端 `go test ./...` 通过；前端 `dart analyze lib/core/services/api/websocket_service.dart lib/features/meeting/pages/meeting_page.dart` 通过。 |

## 2026-04-21 新增功能排查修复（第三轮）
| 项目 | 状态 | 说明 |
|---|---|---|
| WS 动态权限漂移风险 | 已完成 | 增加 `typing/read` 运行时成员校验；若已被移出会话会立即拒绝并自动退订该会话。 |
| WS 封禁会话订阅拦截 | 已完成 | `subscribe` 鉴权从“非解散”收紧为“仅正常会话（status=0）”，屏蔽被封禁会话的订阅。 |
| 验证结果 | 已完成 | 后端再次执行 `go test ./...` 通过。 |

## 2026-04-22 新增问题修复（个人会话）
| 项目 | 状态 | 说明 |
|---|---|---|
| 个人会话清空聊天记录失效 | 已完成 | 根因是消息查询与断线增量同步未使用 `user_chats.cleared_at` 过滤；已在 `GetMessages` 与 `SyncMessages` 增加按当前用户清空时间过滤，清空后不会再拉回历史消息。 |
| 验证结果 | 已完成 | 后端 `go test ./...` 通过。 |

## 2026-04-22 新增能力（TG模式双向清空）
| 项目 | 状态 | 说明 |
|---|---|---|
| 私聊双向清空聊天记录 | 已完成 | 新增后端接口 `POST /chat/:id/clear-both`（仅私聊），可为会话双方同时清空聊天记录。 |
| 清空事件多端同步 | 已完成 | 新增 WS 事件 `chat_history_cleared`，同步更新双方会话列表预览、未读数与会话消息缓存。 |
| 前端交互入口 | 已完成 | 用户资料页清空弹层新增两种模式：`仅为我清空`、`为双方清空（TG模式）`。 |

## 2026-04-22 新增能力（移动端版本更新拦截）
| 项目 | 状态 | 说明 |
|---|---|---|
| 启动页版本检测（Android/iOS） | 已完成 | `SplashPage` 启动阶段读取后端 `app_version_ios/app_version_android` 并与本地版本比较。 |
| 非强制更新提示 | 已完成 | 命中新版本且非强更时弹出“稍后/立即更新”提示，不阻断正常登录流程。 |
| 强制更新阻断 | 已完成 | 命中新版本且配置 `app_force_update=true` 且有更新链接时，阻断进入首页/登录页，仅保留更新入口。 |
| 外部更新链接跳转 | 已完成 | 更新按钮跳转后台配置的 `app_update_url`（外部应用方式打开）。 |
| 强更页回前台自动重检 | 已完成 | 从应用商店返回 App 时会自动触发重检，若已升级可自动放行进入登录/首页。 |
| 更新配置兜底校验 | 已完成 | 当检测到新版本但后台未配置 `app_update_url` 时，跳过更新提示并记录日志，避免“只能提示不能更新”。 |
| 管理后台强更配置校验 | 已完成 | 系统设置页新增校验：开启强制更新时 `app_update_url` 必填，并在开关切换时实时触发链接字段重校验。 |
| 更新字段保存规范化 | 已完成 | 管理后台保存时对 `app_update_url/app_update_message` 自动 `trim`，减少配置空白字符导致的异常。 |

## 2026-04-22 热更新开发进度表（Android/iOS）
| 能力项 | 当前状态 | 完善度 | 说明 |
|---|---|---|---|
| 整包更新配置（后台） | 已开发 | 完善 | 已支持 `app_version_ios/app_version_android/app_force_update/app_update_url/app_update_message` 配置。 |
| 客户端版本检测与拦截（启动页） | 已开发 | 完善 | 已支持非强更提示、强更阻断、更新链接跳转、回前台自动重检。 |
| 强更配置防呆（管理端） | 已开发 | 完善 | 开启强更时更新链接必填，保存字段自动 `trim`。 |
| 代码热更新 SDK 接入（Shorebird/CodePush 等） | 已开发（适配层） | 基本完善 | 已新增 Flutter `HotUpdateSdkAdapter`（MethodChannel 对接 + 未接入时 no-op 降级），并在 `SplashPage` 接入补丁检查与尝试应用流程。 |
| 原生热更新桥接通道（Android/iOS） | 已开发 | 完善 | 已在 Android `MainActivity` 与 iOS `AppDelegate` 接入 `com.genericim/hot_update` 通道，统一支持 `isSupported/applyPatch` 协议，当前返回“SDK未集成”占位结果。 |
| 补丁包发布与灰度机制 | 已开发 | 完善 | 已新增后端补丁模型与管理接口（创建/编辑/发布/暂停/回滚）、客户端补丁检查接口与灰度命中逻辑（按用户/设备哈希）、管理端补丁管理页面。 |
| 说明（当前阶段结论） | 已完成MVP | 完善 | 当前已实现“整包更新 + 强更管控 + 代码补丁发布与灰度（MVP）+ 原生桥接通道”；若需生产落地，还需接入具体原生热更新 SDK（Shorebird/CodePush 等）并完成真实补丁应用能力。 |

## 安全与隐私

| 功能项 | 开发状态 | 完善度 | 说明 |
|---|---|---|---|
| 开启/关闭允许手机号搜索 | 已开发 | 完善 | 前后端均有开关与同步接口。 |
| 开启/关闭平台短号搜索 | 已开发 | 完善 | 已上线独立 `short_id` 字段，支持真实短号精确搜索并受隐私开关控制。 |
| 登录密码修改（短信验证码） | 已开发 | 完善 | 已支持发送验证码并通过验证码修改登录密码。 |
| 设备锁（开启/关闭） | 已开发 | 完善 | 已支持设备锁开关、生效校验、缺省隐私设置自动补齐与登录挑战。 |
| 设备锁：换设备登录需短信验证 | 已开发 | 完善 | 已支持设备锁挑战码（`code=1001`）+ 手机验证码二次验证。 |
| 显示登录过设备列表 | 已开发 | 完善 | 已提供设备列表、活跃会话统计与设备会话管理能力。 |
| 注销账号（短信验证码） | 已开发 | 完善 | 已支持验证码校验后注销；已清理设备/会话/隐私/联系人/好友/会话索引、动态、举报、钱包交易、会员、邀请、通话、表情商店等数据，并物理删除用户。 |
| 注销后“所有信息全部销毁” | 已开发 | 完善 | 已补齐 SQL + Mongo（用户发送消息）+ 本地 uploads + 外部媒体异步清理任务（重试）与审计落库。 |

## 表情商店（一级/二级/三级能力）

| 功能项 | 开发状态 | 完善度 | 说明 |
|---|---|---|---|
| 聊天面板显示表情入口 | 已开发 | 完善 | 聊天消息面板已提供表情入口。 |
| 显示搜索入口、emoji分类、收藏分类 | 已开发 | 完善 | 默认显示 `emoji` 与 `收藏`，并提供搜索入口。 |
| 用户添加的表情组显示在分类后 | 已开发 | 完善 | 已安装表情包按顺序显示在默认分类后。 |
| 点击 emoji 分类查看全部 emoji | 已开发 | 完善 | 支持浏览平台 emoji。 |
| 点击收藏分类查看收藏表情 | 已开发 | 完善 | 支持 emoji 收藏与自定义表情收藏。 |
| 收藏分类首项显示“制作表情”入口 | 已开发 | 完善 | 收藏页第一项固定为制作入口。 |
| 制作表情：显示自定义表情列表 | 已开发 | 完善 | 已有“制作表情”页面与列表管理。 |
| 从相册选择图片并保存制作 | 已开发 | 完善 | 支持从相册选择图片并保存为自定义表情。 |
| 整理自定义表情（排序/删除） | 已开发 | 完善 | 支持拖拽排序、勾选删除。 |
| 平台表情：搜索分类进入商店列表 | 已开发 | 完善 | 可从搜索入口进入表情商店列表。 |
| 商店列表添加一组表情 | 已开发 | 完善 | 支持添加/移除表情包。 |
| 表情详情页点击“添加” | 已开发 | 完善 | 详情弹层支持预览与添加。 |
| 我的表情页面移除已添加表情组 | 已开发 | 完善 | 支持移除。 |
| 表情管理：排序已添加表情组 | 已开发 | 完善 | 支持拖拽排序。 |
| 自定义表情发送（本地文件） | 已开发 | 完善 | 选择自定义表情可直接发送图片消息。 |
| 自定义表情发送（跨设备 URL） | 已开发 | 完善 | 支持远端 URL 发送链路，并增强云端自定义表情路径过滤，避免同步不可跨设备本地路径。 |
| 表情商店云同步（已装包/收藏/自定义） | 已开发 | 完善 | 已有 `/user/emoji-store` GET/PUT，支持冲突基线时间、收藏与自定义一致性校验。 |
| 云同步入参校验与清洗 | 已开发 | 完善 | 已增加 payload 大小保护、数量上限、字段过滤与规范化，并校验收藏引用与有效表情包。 |
| 平台表情包后台可配置化 | 已开发 | 完善 | 已新增后端目录模型与接口（`GET /user/emoji-store/catalog`），支持目录更新时间输出与前端动态拉取。 |

## 正在输入消息优化

| 功能项 | 开发状态 | 完善度 | 说明 |
|---|---|---|---|
| 单聊对方输入时提醒到会话 | 已开发 | 完善 | 会话列表与会话内均可感知。 |
| 群聊某人正在输入提醒到会话 | 已开发 | 完善 | 支持群聊输入提醒。 |
| 多人同时输入显示最后输入用户 | 已开发 | 完善 | 采用“最后输入用户”覆盖更新策略。 |
| 8秒未发送或取消发送则取消提醒 | 已开发 | 完善 | 发送端与接收端均有 8 秒超时清理。 |

## 声网群会议（MVP）

| 功能项 | 开发状态 | 完善度 | 说明 |
|---|---|---|---|
| 会议数据模型（会议/成员/邀请） | 已开发 | 完善（后端） | 已新增 `meetings`、`meeting_participants`、`meeting_invites` 并接入自动迁移。 |
| 创建会议（支持群会话上下文） | 已开发 | 完善 | `POST /meeting/create`，支持 `voice/video`、最大参会人数、批量邀请。 |
| 加入会议 | 已开发 | 完善 | `POST /meeting/join`，支持被邀请加入与群成员加入。 |
| 离开会议 | 已开发 | 完善 | `POST /meeting/leave`，主持人离开或房间空时自动结束会议。 |
| 邀请成员 | 已开发 | 完善 | `POST /meeting/invite`，主持人可在会中追加邀请。 |
| 结束会议 | 已开发 | 完善 | `POST /meeting/end`，主持人结束并同步成员状态。 |
| 会议 Token | 已开发 | 完善 | `GET /meeting/token`，按会议频道生成 RTC Token。 |
| 会议详情查询 | 已开发 | 完善 | `GET /meeting/detail`，返回会议状态与成员列表。 |
| 会议实时事件通知 | 已开发 | 完善 | 已推送 `meeting_invite / meeting_member_joined / meeting_member_left / meeting_ended`。 |
| 会议前端 UI 与交互页 | 已开发 | 完善（前端） | 已新增会议页面、成员列表、加入/离开/邀请/结束操作。 |
| 群聊/频道入口发起会议 | 已开发 | 完善（前端） | 聊天页顶部已支持语音/视频会议发起并跳转会议页。 |
| 前端会议 API 封装 | 已开发 | 完善（前端） | 已新增 `meeting_service.dart`，覆盖 create/join/leave/invite/end/token/detail。 |
| 前端 WS 会议事件消费 | 已开发 | 完善（前端） | 已接入 `meeting_invite/member_joined/member_left/ended` 事件并自动刷新会议页。 |
| 会议推送点击跳转 | 已开发 | 完善（前端） | 推送 `meeting_invite` 点击可直达会议页。 |
| 前端 Agora 会中连麦 | 已开发 | 完善（前端） | 已接入会议页 RTC 自动入会/退会、远端 UID 流渲染、本地静音/视频开关/切换摄像头。 |
| 主持人会控能力（静音/移出/转移主持人） | 已开发 | 完善 | 已新增后端会控 API 与前端成员操作菜单，支持会中管理。 |
| 会中稳定性（断线重连/Token 续期/被移出处理） | 已开发 | 完善 | 已增加 RTC 自动重连、Token 续期、WS 重连后状态恢复、被移出自动断开与提示。 |
| 会中体验优化（昵称映射/主持人静音锁） | 已开发 | 完善 | 已返回成员 `agora_uid` 并映射远端昵称；被主持人静音/关视频时前端禁止自行解除并给出提示。 |
| 会控安全闭环（被移出后权限收敛） | 已开发 | 完善 | 后端限制被移出成员直接重入/查看详情，前端收到踢出事件后自动断开并退出会议页。 |

## 本轮新增（相对上一版）

| 项目 | 状态 |
|---|---|
| 表情商店云同步接口（`/user/emoji-store`） | 已完成 |
| 自定义表情跨设备发送链路（本地/远端 URL） | 已完成 |
| 注销账号清理范围补强（含表情商店数据） | 已完成 |
| 搜索“平台短号”隐私拦截落地（临时以用户名精确匹配承接） | 已完成 |
| 平台短号升级为真实短号字段（`short_id`） | 已完成 |
| 云同步接口服务端校验（大小/数量/字段清洗） | 已完成 |
| 云同步清洗逻辑单元测试补充（上限/去重/过滤/时间规范化） | 已完成 |
| 表情包目录后台管理 API（列表/新增/编辑/上下架/删除） | 已完成 |
| 表情包目录管理后台页面（可视化增删改查） | 已完成 |
| 云同步冲突策略升级（基线时间 + 冲突自动合并重试） | 已完成 |
| 注销账号全链路清理扩展（动态/钱包/会员/邀请/举报/通话等） | 已完成 |
| 注销账号跨存储清理扩展（Mongo 消息 + 本地 uploads 文件） | 已完成 |
| 注销账号清理审计落库（`account_deletion_audits`） | 已完成 |
| 设备/会话终止后按设备失效登录态（缓存标记） | 已完成 |
| 注销账号外部媒体异步清理任务（重试机制） | 已完成 |
| 表情云同步一致性增强（有效包过滤/收藏引用校验） | 已完成 |
| 声网群会议后端 MVP（模型/路由/API/实时事件） | 已完成 |
| 声网群会议前端基础版（API/页面/入口/WS） | 已完成 |
| 声网群会议前端 RTC 基础版（Agora 入会/渲染/基础控制） | 已完成 |
| 声网群会议主持人会控（后端+前端） | 已完成 |
| 声网群会议会中稳定性增强（重连/续期/踢出恢复） | 已完成 |
| 声网群会议会中体验增强（昵称映射/主持人静音锁） | 已完成 |
| 声网群会议会控安全闭环（踢出后不可直返） | 已完成 |

## 未完成与建议下一步

| 项目 | 优先级 | 建议 |
|---|---|---|
| 平台表情包管理后台（运营可视化） | 已完成 | 已完成后台 API + 管理端页面，支持列表/新增/编辑/上下架/删除。 |
| 注销账号对象存储/CDN 异步实删专项 | 已完成 | 已支持外部媒体异步清理任务表与失败重试机制（HTTP DELETE）。 |
| 声网群会议会中能力增强（静音/踢人/角色切换） | 已完成 | 已完成主持人会控接口与前端会控面板。 |
| 声网群会议 Agora 引擎渲染与重连策略 | 已完成 | 已完成多人 RTC 画面、音视频开关、重连与续期恢复。 |

## 验证结果（2026-04-21）

| 项目 | 结果 | 说明 |
|---|---|---|
| 后端编译与测试（`go test ./...`） | 通过 | `cmd/server` 与 `internal/handlers` 等模块均通过，当前代码可编译。 |
| 表情包目录后台管理 API 编译验证（`go test ./...`） | 通过 | 新增 `admin/emoji-store/packs` 相关接口已通过编译测试。 |
| 云同步冲突策略编译验证（`go test ./...`） | 通过 | 冲突检测与返回体改动已通过后端测试。 |
| 云同步冲突策略前端静态检查（`dart analyze`） | 通过（有既有 warning） | 本次改动无 error；仅有 API 客户端历史 warning 与 1 个 deprecation info。 |
| 注销账号清理扩展编译验证（`go test ./...`） | 通过 | 删除链路扩展后已完成全量后端编译测试。 |
| 注销账号跨存储清理测试（`go test ./...`） | 通过 | 新增本地上传路径安全映射与去重删除测试，`internal/handlers` 已通过。 |
| 平台短号升级编译验证（`go test ./...`） | 通过 | 短号字段、注册分配、搜索与隐私拦截改动已通过后端测试。 |
| 注销账号清理审计落库编译验证（`go test ./...`） | 通过 | 新增审计模型并接入注销清理结果落库，后端测试通过。 |
| 基本完善项升级编译验证（`go test ./...`） | 通过 | 设备会话失效标记、外部媒体异步清理重试、表情云同步一致性增强均已通过后端测试。 |
| 声网群会议后端 MVP 编译验证（`go test ./...`） | 通过 | 新增 `meeting` 模型、handler、路由与迁移已通过后端全量编译。 |
| 声网群会议前端核心文件静态检查（`dart analyze`） | 通过 | `meeting_service.dart` 与 `meeting_page.dart` 无 error/warning。 |
| 声网群会议前端联动静态检查（`dart analyze`） | 通过（有既有 warning/info） | 本次改动无 error；主要为历史 `chat_detail_page` 与 `websocket_service` 既有提示。 |
| 声网群会议会控接口编译验证（`go test ./...`） | 通过 | 新增 `/meeting/member/mute`、`/meeting/member/kick`、`/meeting/host/transfer` 已通过后端编译。 |
| 声网群会议稳定性静态检查（`dart analyze`） | 通过 | 会议页重连/续期/被移出处理改动已通过静态检查。 |
| 声网群会议体验优化验证（`go test ./...` + `dart analyze`） | 通过 | `agora_uid` 返回、远端昵称映射、主持人静音锁与重连提示均已通过编译/静态检查。 |
| 声网群会议踢出安全闭环验证（`go test ./...` + `dart analyze`） | 通过 | 被移出成员不可直接重入/查看详情，前端会自动退出会议页。 |
| 会议模块后端全量回归（2026-04-22，`go test ./...`） | 通过 | 后端全量测试通过，会议模块与其他模块编译/测试正常。 |
| 会议模块处理器专项测试（2026-04-22，`go test ./internal/handlers -run Meeting -count=1`） | 通过 | 会议处理器专项用例通过。 |
| 会议模块前端静态检查（2026-04-22，`dart analyze`） | 通过 | `meeting_page.dart`、`meeting_service.dart`、`websocket_service.dart` 无 issue。 |
| 管理端系统设置页静态检查（2026-04-22，`npm run lint -- src/views/system/settings/index.vue`） | 通过 | 已补齐依赖并修复格式/unused 变量问题，lint 通过。 |
| Flutter 全仓库静态检查（2026-04-22，`dart analyze`） | 通过 | 已完成静态检查基线治理（生成文件排除 + 历史遗留规则统一收敛），当前全仓库 `dart analyze` 输出 `No issues found!`。 |
| 热更新后端全量编译测试（2026-04-22，`go test ./...`） | 通过 | 新增热更新模型、管理接口、灰度命中逻辑后执行后端全量测试，结果通过。 |
| 热更新 Flutter 定向静态检查（2026-04-22，`dart analyze lib/core/services/api/hot_update_service.dart lib/core/services/hot_update_sdk_adapter.dart lib/features/splash/pages/splash_page.dart`） | 通过 | 热更新服务、SDK 适配层与启动页接入改动静态检查通过。 |
| 热更新管理端页面静态检查（2026-04-22，`npm run lint -- src/views/system/hot-update/index.vue src/router/modules/system.ts`） | 通过 | 新增“热更新补丁”管理页与系统路由入口，lint 通过。 |
| 热更新 Android 原生编译检查（2026-04-22，`gradlew app:compileDebugKotlin`） | 通过 | 新增 `MainActivity` 热更新 MethodChannel 桥接代码后，Android Kotlin 编译通过（仅有第三方依赖 deprecation 提示）。 |

## 2026-04-22 热更新闭环补全（补丁上报 + 报表查询）
| 项目 | 状态 | 完善度 | 说明 |
|---|---|---|---|
| App 补丁应用状态上报 `/app/hot-update/report` | 已开发 | 完善 | 已新增 `check_hit / sdk_not_available / sdk_not_integrated / apply_failed / apply_success` 上报接口，支持 `patch_id/patch_ref_id` 反查补丁，并记录 `platform/channel/app_version/build_number/device_id/user_uuid/client_ip`。 |
| 管理端报表查询 `/admin/hot-update/reports` | 已开发 | 完善 | 已支持分页、按状态、平台、渠道、patch、用户/设备条件过滤，并支持关键词查询。 |
| 热更新补丁上报模型 `hot_update_patch_reports` | 已开发 | 完善 | 已新增模型与自动迁移，包含 patch 关联字段、状态、终端信息及上报时间索引。 |
| Flutter 启动页热更新上报闭环 | 已开发 | 完善 | `SplashPage` 命中补丁后先上报 `check_hit`，并按 SDK 状态与应用结果上报对应状态；`HotUpdateService` 新增 `reportPatchResult(...)`。 |
| 管理端 API 定义补全（报表查询） | 已开发 | 完善 | `admin/src/api/admin.ts` 已新增报表类型与 `getHotUpdatePatchReports`。 |

## 本轮验证结果（2026-04-22）
| 项目 | 结果 | 说明 |
|---|---|---|
| 后端全量编译/测试：`go test ./...` | 通过 | 新增热更新上报模型、接口与路由后，后端全量测试通过。 |
| Flutter 定向静态检查：`dart analyze lib/core/services/api/hot_update_service.dart lib/features/splash/pages/splash_page.dart` | 通过 | 无 error/warning，热更新上报流程静态检查通过。 |
| Flutter 全量静态检查：`dart analyze` | 通过 | 全仓分析通过，输出 `No issues found!`。 |
| Android 原生编译检查：`./gradlew.bat app:compileDebugKotlin` | 通过 | Android Kotlin 编译通过（仅有 Gradle/插件 deprecation 提示）。 |
| 管理端 API lint：`npm run lint -- src/api/admin.ts` | 通过 | `admin/src/api/admin.ts` 历史 Prettier 存量问题已清理并通过 lint。 |

## 2026-04-22 收尾完成度
| 维度 | 结果 | 说明 |
|---|---|---|
| 本轮剩余 3% 收尾 | 已完成 | 已完成 lint 收口、全量分析与原生编译验证。 |
| 当前阶段完成度 | 100% | 当前迭代范围内无未完成项。 |

## 2026-04-22 真实热更新 SDK 接入（Shorebird）
| 项目 | 状态 | 完善度 | 说明 |
|---|---|---|---|
| Flutter 端 Shorebird SDK 依赖接入 | 已开发 | 完善 | 已新增 `shorebird_code_push` 依赖并完成依赖解析。 |
| 热更新适配层接入 Shorebird 主链路 | 已开发 | 完善 | `HotUpdateSdkAdapter` 已优先使用 Shorebird `checkForUpdate/update`，并保留原 MethodChannel 兜底。 |
| 热更新通道映射 | 已开发 | 完善 | 已按后端 `channel` 动态映射 Shorebird `UpdateTrack`（默认 `stable`）。 |
| 运行时可用性与降级 | 已开发 | 完善 | 当非 Shorebird 引擎环境（如 `flutter run/build`）时自动降级并返回 `hot_update_sdk_not_integrated`。 |
| 关键验证 | 已完成 | 完善 | `dart analyze`（热更新相关文件）通过；Android `./gradlew.bat app:compileDebugKotlin` 通过。 |

> 说明：Shorebird 热更新能力在使用 `shorebird release/shorebird preview` 构建的包中生效；普通 `flutter run/build` 环境会自动走降级逻辑。

## 2026-04-22 管理端（Admin）Lint 收口补充
| 项目 | 状态 | 说明 |
|---|---|---|
| Admin 全量 Lint（`npm run lint`） | 已完成 | 已修复存量 `prettier / no-unused-vars / no-empty` 等问题，当前全量 lint 通过。 |
| Admin 生产构建（`npm run build`） | 已完成 | 构建通过，产物正常输出到 `admin/dist`。 |

## 2026-04-22 全链路回归补充
| 项目 | 结果 | 说明 |
|---|---|---|
| 后端全量编译/测试：`go test ./...` | 通过 | 在本轮 Admin 收口后复验，后端全量通过，无新增回归。 |
| Flutter 全量静态检查：`dart analyze` | 通过 | 在本轮 Admin 收口后复验，输出 `No issues found!`。 |

## 2026-04-22 客服管理端（admin-kf）补充
| 项目 | 状态 | 说明 |
|---|---|---|
| 依赖安装（`npm --prefix admin-kf install`） | 已完成 | 已补齐 `admin-kf` 本地依赖，解决 `vite is not recognized` 构建阻塞。 |
| 生产构建（`npm --prefix admin-kf run build`） | 已完成 | 构建通过，产物正常输出到 `admin-kf/dist`；存在大包体积提示（非阻塞）。 |
| 分包优化（`admin-kf/vite.config.ts` manualChunks） | 已完成 | 已拆分 `vue-vendor/iconify/vendor/element-plus` 分包并复测通过；当前仍有 `element-plus` 大包提示，需按需引入组件才能进一步降低。 |

## 2026-04-22 其他端回归补充（Windows/iOS/macOS）
| 项目 | 结果 | 说明 |
|---|---|---|
| Flutter 测试用例检查：`flutter test` | 通过（无用例） | 当前仓库无 `test/` 目录，命令返回 `Test directory "test" not found.`，属于“未配置自动化测试集”而非编译错误。 |
| Windows 构建初检：`flutter build windows` | 失败（已修复） | 初次失败原因为 `build/windows/x64/extracted/firebase_cpp_sdk_windows` 缺失且本地 zip 损坏。 |
| Windows 构建修复 | 已完成 | 已重新下载 `firebase_cpp_sdk_windows_12.7.0.zip`（完整包）并重新解压 SDK。 |
| Windows 端复测：`flutter build windows` | 通过 | 已成功产出 `build/windows/x64/runner/Release/genericim.exe`。 |
| iOS 构建命令：`flutter build ios` | 不适用（当前环境） | 当前环境命令返回 `Could not find a subcommand named "ios"`，需在 macOS + Xcode 环境进行 iOS 真机构建验证。 |
| macOS 构建命令：`flutter build macos` | 不适用（当前环境） | 当前环境命令返回 `Could not find a subcommand named "macos"`，需在 macOS 环境进行桌面构建验证。 |
| Flutter 环境确认：`flutter doctor -v` | 通过 | 当前开发机为 Windows，`flutter doctor -v` 无异常（`No issues found!`）。 |

## 2026-04-22 全新增功能代码复排（第二轮）
| 项目 | 结果 | 说明 |
|---|---|---|
| 后端全量测试：`go test ./...` | 通过 | 后端新增功能相关模块（含会议、热更新、安全隐私、表情商店）全量测试通过。 |
| 会议专项测试：`go test ./internal/handlers -run Meeting -count=1` | 通过 | 会议处理链路专项复测通过。 |
| 热更新专项测试：`go test ./internal/handlers -run HotUpdate -count=1` | 通过 | 热更新补丁链路专项复测通过。 |
| Flutter 全量静态检查：`dart analyze` | 通过 | 输出 `No issues found!`。 |
| Flutter Web 构建：`flutter build web` | 通过 | 构建成功；存在 Wasm dry-run 兼容性提示（当前 Web 构建非阻塞）。 |
| Flutter Android Debug 包：`flutter build apk --debug` | 通过 | 成功产出 `build/app/outputs/flutter-apk/app-debug.apk`。 |
| Flutter Windows 构建：`flutter build windows` | 通过 | 成功产出 `build/windows/x64/runner/Release/genericim.exe`。 |
| Android 原生编译：`./gradlew.bat app:compileDebugKotlin` | 通过 | Kotlin 编译通过；仅有 Gradle 过时特性警告（非阻塞）。 |
| Admin Lint：`npm --prefix admin run lint` | 通过 | 管理端全量 lint 通过。 |
| Admin Build：`npm --prefix admin run build` | 通过 | 管理端生产构建通过。 |
| Admin-KF 脚本检查：`npm --prefix admin-kf run` | 通过（能力确认） | 当前仅有 `dev/build/preview`，未提供 `lint` 脚本。 |
| Admin-KF Build：`npm --prefix admin-kf run build` | 通过 | 客服管理端构建通过；`element-plus` 大包提示仍为非阻塞项。 |

> 结论：本轮“所有新增功能代码排查”未发现阻塞发布级错误；当前剩余为优化类事项（Admin-KF 缺少 lint 脚本、Web Wasm 兼容性提示、部分构建体积/过时警告）。

## 2026-04-23 群会议审批与群内入口（继续开发）
| 项目 | 状态 | 完善度 | 说明 |
|---|---|---|---|
| 群聊内显示“进行中会议”入口 | 已开发 | 完善 | 群聊页新增会议横幅，支持一键进入会议；并通过 WS 事件自动刷新。 |
| 群成员加入会议审批流 | 已开发 | 完善 | 成员可发起入会申请；主持人可同意/拒绝；申请人与会中成员可收到实时状态事件。 |
| 发起会议邀请列表（群成员多选） | 已开发 | 完善 | 发起群会议时支持群成员检索+多选邀请，不再需要手输 UUID。 |
| 会议活跃状态接口 | 已开发 | 完善 | 新增 `GET /meeting/active`，用于群聊页实时展示当前活跃会议。 |
| 入会申请审批接口 | 已开发 | 完善 | 新增 `POST /meeting/join-request/review`，主持人审批申请加入。 |
| 会议事件补齐 | 已开发 | 完善 | 新增/接入 `meeting_started`、`meeting_join_request`、`meeting_join_request_reviewed`。 |

## 2026-04-23 编译与回归结果
| 项目 | 结果 | 说明 |
|---|---|---|
| 后端编译 | 通过 | `go build ./cmd/server` 成功。 |
| 后端全量测试 | 通过 | `go test ./...` 成功。 |
| Flutter 全量静态检查 | 通过 | `flutter analyze` 输出 `No issues found!`。 |
| Android Debug 打包（首次） | 失败（已修复） | 原因是本地 Gradle/NDK 构建缓存异常（`.cxx` 缺失 + dex 合并失败）。 |
| Android Debug 打包（修复后） | 通过 | 执行 `flutter clean` + `flutter pub get` 后，`flutter build apk --debug` 成功，产物：`build/app/outputs/flutter-apk/app-debug.apk`。 |
