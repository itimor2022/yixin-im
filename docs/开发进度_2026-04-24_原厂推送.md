# 开发进度（2026-04-24）- Android 原厂推送

## 本轮完成
| 项目 | 状态 | 说明 |
|---|---|---|
| Android 原厂推送桥接 | 已完成 | 新增 vendor push 初始化、SDK 可用性检测、请求 token 的 MethodChannel。 |
| HMS token 获取 | 已完成（SDK 就绪时生效） | 原生侧已接入拉取逻辑并回传 Flutter。 |
| 小米 regId 获取 | 已完成（SDK 就绪时生效） | 原生侧已接入注册与 regId 拉取并回传 Flutter。 |
| OPPO 客户端 token 获取 | 已完成（反射接入） | 新增 OPPO 注册回调反射实现 + 注册ID轮询兜底。 |
| Flutter 联动上报 | 已完成 | 收到 vendor token 后上报 `/user/push-token`，含 `push_channel`。 |
| Token 上传补偿重试 | 已完成 | 修复“同 token 直接跳过上传”问题；失败后改为后台持续重试（指数退避），直到上传成功。 |
| 前台保活 token 同步 | 已完成 | App 回到前台时自动做轻量同步检查；已同步且在 6 小时内则跳过，减少无效请求。 |
| 原厂 token 前台补拉 | 已完成 | Android 前台保活时，如目标通道非 FCM 且当前未拿到目标通道 token，会按 30 分钟节流自动补拉。 |
| Vendor SDK 开关强校验 | 已完成 | 当 `ENABLE_VENDOR_PUSH_SDK=true` 时，缺失 SDK 坐标/厂商密钥参数将直接构建失败（Fail Fast）。 |
| Vendor 参数环境变量注入 | 已完成 | Android 构建参数支持同名环境变量读取，便于本地与 CI 不改仓库文件注入密钥。 |
| 后端通道路由单测 | 已完成 | 新增 `NormalizePushChannel` 测试，覆盖别名与按设备类型默认回退规则。 |
| 后端推送配置保存校验 | 已完成 | 系统设置中启用通道时，后端会校验必填项（含 FCM JSON 格式），阻止无效配置落库。 |
| 推送配置解析单测 | 已完成 | 新增设置解析/布尔转换单测，覆盖 `parseSettingBoolValue` 与 `normalizeSettingInputValue`。 |
| 管理端推送配置前置校验 | 已完成 | 管理端保存功能设置前先校验 APNs/FCM/HMS/小米/OPPO 必填项与 FCM JSON，减少无效提交。 |
| 原厂推送配置模板 | 已完成 | 新增 `android/gradle.vendor.sample.properties`，可按模板填参后复制到 `gradle.properties`。 |
| 本地私有配置文件支持 | 已完成 | 支持 `android/gradle.vendor.local.properties`（已忽略提交）注入原厂推送参数，便于本地开发。 |
| 小米仓库地址多来源读取 | 已完成 | 根构建脚本支持 `XIAOMI_PUSH_MAVEN_REPO` 按 `-P/gradle.properties`、环境变量、`gradle.vendor.local.properties` 优先级读取。 |
| 原厂推送预检脚本 | 已完成 | 新增 `scripts/android_vendor_push_preflight.ps1`，可一键检查参数缺项、来源优先级，并可选联动编译探测。 |
| 生产模板版本落地 | 已完成 | `android/gradle.vendor.sample.properties` 已填入可用基线版本（HMS/Xiaomi/OPPO）。 |
| Vendor 开关多来源生效 | 已完成 | `ENABLE_VENDOR_PUSH_SDK` 已支持 `-P` / 环境变量 / `gradle.vendor.local.properties`，避免被仓库默认值锁死。 |
| Android 角标同步 | 已完成 | 新增移动端角标服务，未读数变化时同步更新 Android/iOS 角标；并与桌面托盘/桌面角标统一联动。 |
| Android 原厂与 FCM 防覆盖 | 已完成 | 原厂通道可用时不再被 FCM token 覆盖；原厂连续失败会自动回退 FCM，避免 token 漂移导致收不到推送。 |
| Android 构建配置占位 | 已完成 | 增加 `PUSH_HMS_APP_ID / PUSH_XIAOMI_APP_ID / PUSH_XIAOMI_APP_KEY / PUSH_OPPO_APP_KEY / PUSH_OPPO_APP_SECRET`。 |
| Vendor SDK 可开关依赖 | 已完成 | 新增 `ENABLE_VENDOR_PUSH_SDK`，默认关闭，避免影响当前稳定编译。 |
| 后端 OPPO 推送实发 | 已完成 | 已补齐 OPPO 鉴权、单推、auth token 缓存与失效重试。 |

## 验证结果
| 命令 | 结果 |
|---|---|
| `go test ./...`（backend） | 通过 |
| `flutter analyze` | 通过 |
| `flutter analyze lib/core/services/push_notification_service.dart` | 通过 |
| `./gradlew.bat app:compileDebugKotlin` | 通过 |
| `ENABLE_VENDOR_PUSH_SDK=true` + 环境变量注入后 `./gradlew.bat app:compileDebugKotlin` | 通过 |
| `flutter build apk --debug` | 通过 |

## 当前剩余事项
| 项目 | 优先级 | 说明 |
|---|---|---|
| 三方 SDK 正式依赖坐标落地 | 高 | 将华为/小米/OPPO 官方 SDK 坐标填入 `gradle.properties` 并开启 `ENABLE_VENDOR_PUSH_SDK=true`。 |
| 小米仓库源确认 | 中 | 如默认仓库无法拉取，按已支持的多来源方式填入 `XIAOMI_PUSH_MAVEN_REPO` 并在目标环境验证。 |
| 真机端到端联调 | 高 | 分别在华为/小米/OPPO 真机验证 token 获取、上报、服务端推送送达。 |

## 继续排查（2026-04-25）
| 项目 | 结果 | 说明 |
|---|---|---|
| Android 原厂推送预检（默认配置） | 通过 | `ENABLE_VENDOR_PUSH_SDK=false` 下可正常构建，未配置厂商密钥时自动走 FCM 基线。 |
| Android 原厂推送预检（强制 Vendor） | 预期失败 | `-ForceVendorEnabled` 下因缺少 `HMS/Xiaomi/OPPO` 必填参数被拦截，Fail Fast 生效。 |
| Android 编译链路 | 通过 | `./gradlew.bat app:compileDebugKotlin` 通过。 |
| Flutter 推送/角标代码静态检查 | 通过 | `flutter analyze lib/app.dart lib/core/services/push_notification_service.dart lib/core/services/app_badge_service.dart` 无问题。 |
| 后端推送配置校验测试 | 通过 | `go test ./internal/handlers`、`go test ./internal/services/...` 通过。 |
| 管理端配置页构建 | 通过 | `npm --prefix admin run build` 通过。 |
| 预检脚本增强（FCM 基线） | 已完成 | `android_vendor_push_preflight.ps1` 新增 `applicationId / google-services.json / project_id / package_name` 输出，便于快速定位包名与 Firebase 配置问题。 |
| 设备推送诊断字段（后端） | 已完成 | `GetDevices` 与管理端用户列表接口新增 `push_channel / push_token_bound / push_token_length`，便于快速定位 token 未绑定或通道错误问题。 |
| 管理端推送状态可视化 | 已完成 | 用户管理页“设备 / IP”列增加 Push 通道与 token 绑定状态展示，后台可直接判断设备是否已成功上报推送。 |
| Android 默认消息通知渠道 | 已完成 | 新增 `genericim_messages` 高优先级通知渠道，并在 Manifest 声明为 FCM 默认渠道，降低 Android 8+ 通知不弹/优先级异常风险。 |

## 当前结论（2026-04-25）
- 代码层面未发现新的阻塞问题。
- 当前环境下“原厂推送未生效”的核心原因是：厂商 SDK 坐标与密钥尚未完整注入，系统按设计回退到 FCM。
- Android 角标能力已接入；是否显示数字取决于 ROM/Launcher 支持，不属于代码缺陷。

## 关键文件
- [MainActivity.kt](/e:/yi-xin-ai2k/android/app/src/main/kotlin/com/ranxin/jstx/www/MainActivity.kt)
- [push_notification_service.dart](/e:/yi-xin-ai2k/lib/core/services/push_notification_service.dart)
- [push_service.go](/e:/yi-xin-ai2k/backend/internal/services/push_service.go)
- [build.gradle.kts](/e:/yi-xin-ai2k/android/app/build.gradle.kts)
- [build.gradle.kts](/e:/yi-xin-ai2k/android/build.gradle.kts)
- [gradle.properties](/e:/yi-xin-ai2k/android/gradle.properties)
- [proguard-rules.pro](/e:/yi-xin-ai2k/android/app/proguard-rules.pro)
- [android_vendor_push_setup.md](/e:/yi-xin-ai2k/docs/android_vendor_push_setup.md)
