# 通用IM 20260624 测试记录

## 测试环境

- 包名：`com.genericim.ma100`
- 后端：`http://127.0.0.1:8080`
- 模拟器访问后端：`http://10.0.2.2:8080`
- 设备：
  - `127.0.0.1:16384` / `ALN-AL00`
  - `127.0.0.1:16416` / `V2366GA`
  - `127.0.0.1:16448` / `PJJ110`
- 主要账号：`smoke_alice`、`smoke_bob`
- 证据目录：`release-archives\qa-20260624`

## 最新结论

- P0 已通过：消息可靠性、通话二次拉起、HOME 后台恢复、强杀恢复均通过。
- P1 已通过：Android 全量 QA + Monkey、双设备全流程、写入流、多设备 WS、未读推送一致性、搜索分页高亮均通过。
- 本轮发现 1 个测试脚本误报并已修复：系统通知/快捷设置面板覆盖 App 时，`fast_real_device_full_suite.ps1` 会误判消息页失败；已增加 `cmd statusbar collapse`，复测 16/16 通过。
- 未发现 App 级 `FATAL EXCEPTION`、`ANR`、`FlutterError`、`RenderFlex`、`message failed`、`call failed`。
- 剩余非阻断风险：模拟器 logcat 仍有 HMS/GMS/系统服务噪声，真实厂商推送和真实音视频媒体链路仍需真机专项验证。

## P0 执行记录

### 消息可靠性 + 通话二次拉起 + 后台恢复

命令：

```powershell
& 'pwsh' -NoProfile -File .\scripts\reliability_call_resume_suite.ps1 -BurstCount 20 -SendDelayMs 1200 -Api429RetryCount 1 -CallCycles 3
```

报告：

- `release-archives\qa-20260624\reliability-call-resume-20260624-203127\report.md`
- `release-archives\qa-20260624\reliability-call-resume-20260624-203127\results.json`

结果：

- PASS=7，FAIL=0，WARN=2。
- PASS：后端健康。
- PASS：`127.0.0.1:16416`、`127.0.0.1:16448` 都能访问 `10.0.2.2:8080`。
- PASS：Alice/Bob 各 20 条，共 40 条消息，发送和列表拉取无缺失。
- PASS：通话二次拉起 3 轮通过，覆盖创建、拒绝、再创建、取消、视频接通、挂断。
- PASS：HOME 后台恢复后双方均能看到最新消息。
- PASS：强杀重启后双方均能看到最新消息。
- WARN：两台设备 logcat 有 error-like 噪声，未命中 App 崩溃/ANR 级别失败。

P0 判定：通过。没有发现消息突然消失、通话无法二次拉起、后台/强杀恢复后消息不可见。

## P1 执行记录

### Android 全量 QA + Monkey

命令：

```powershell
& 'pwsh' -NoProfile -File .\scripts\full_android_qa_suite.ps1 -SkipInstall -MonkeyEvents 180
```

报告：

- `release-archives\qa-20260624\android-full-qa-20260624-203332\report.md`
- `release-archives\qa-20260624\android-full-qa-20260624-203332\results.json`

结果：

- PASS=15，FAIL=0，WARN=3，INFO=5。
- PASS：三台设备后端连通。
- PASS：API 私聊消息双向发送和拉取成功。
- PASS：API 语音拒绝、视频接通和挂断通过。
- PASS：两台主测设备登录态/主界面状态正常。
- PASS：两台主测设备重启 App 后消息列表/详情仍包含最新 QA 文本。
- PASS：聊天详情语音/视频入口可见。
- PASS：三台设备 Monkey 180 events 未出现明确 crash/ANR marker。
- WARN：三台设备 logcat 有模拟器系统/HMS/GMS 类噪声。

### 双设备真实快速操作全流程

首次运行：

- 报告：`release-archives\qa-20260624\p1-full-flow-rerun\report.md`
- 结果：total=16，pass=12，fail=3，tool_warn=1。
- 原因：B 设备被 Android 系统通知/快捷设置面板覆盖，截图停留在系统层，不是 App 页面缺失。

修复：

- 文件：`scripts\fast_real_device_full_suite.ps1`
- 处理：`Ensure-AppForeground` 增加 `cmd statusbar collapse`，每个 case 前收起系统面板并重新拉起 App。

复测命令：

```powershell
& 'pwsh' -NoProfile -File .\scripts\fast_real_device_full_suite.ps1 -BaseDir release-archives\qa-20260624\p1-full-flow-after-statusbar-fix -DeviceA 127.0.0.1:16416 -DeviceB 127.0.0.1:16448
```

复测报告：

- `release-archives\qa-20260624\p1-full-flow-after-statusbar-fix\report.md`
- `release-archives\qa-20260624\p1-full-flow-after-statusbar-fix\latest-full-suite-summary.json`

复测结果：

- total=16，pass=16，fail=0，tool_warn=0。
- fatal_pattern_count=0，app_error_pattern_count=0。
- 覆盖：设置、钱包、充值只读、提现零余额、账单/密码入口、消息列表、私聊详情、附件面板、长按菜单、群聊、联系人/发现/设置 tab、通话按钮可见。

### 写入流

命令：

```powershell
& 'pwsh' -NoProfile -File .\scripts\fast_real_device_write_flows.ps1 -BaseDir release-archives\qa-20260624\p1-write-flow-rerun -DeviceB 127.0.0.1:16448
```

报告：

- `release-archives\qa-20260624\p1-write-flow-rerun\report.md`
- `release-archives\qa-20260624\p1-write-flow-rerun\latest-write-flows-summary.json`

结果：

- total=3，pass=3，fail=0。
- fatal=0，app_error=0。
- 私聊写入文本：`codexpriv20260624-204303`
- 群聊写入文本：`codexgroup20260624-204303`

### 多设备 WebSocket 一致性

命令：

```powershell
& 'pwsh' -NoProfile -File .\scripts\smoke_multidevice_ws_consistency.ps1 -OutputPath release-archives\qa-20260624\multidevice-ws-consistency.json
```

结果文件：`release-archives\qa-20260624\multidevice-ws-consistency.json`

结果：

- sent_seq=11。
- device_a_unread=11。
- device_b_unread=11。
- device_a_last_msg_seq=11。
- device_b_last_msg_seq=11。

### 未读数 + 推送绑定一致性

命令：

```powershell
& 'pwsh' -NoProfile -File .\scripts\stress_unread_push_consistency.ps1 -MessageCount 20 -SendDelayMs 300 -OutputPath release-archives\qa-20260624\unread-push-consistency.json
```

结果文件：`release-archives\qa-20260624\unread-push-consistency.json`

结果：

- message_count=20。
- device_a_unread_after_send=20。
- device_b_unread_after_send=20。
- device_a_unread_after_read=0。
- device_b_unread_after_read=0。
- push_tokens_bound=2。

### 搜索分页和高亮

命令：

```powershell
& 'pwsh' -NoProfile -File .\scripts\smoke_search_pagination_highlight.ps1 -OutputPath release-archives\qa-20260624\search-pagination-highlight.json
```

结果文件：`release-archives\qa-20260624\search-pagination-highlight.json`

结果：

- contact_count=1。
- chat_count=1。
- page1_count=2，page1_has_more=true。
- page2_count=2。
- highlight_text=`global sps24204425 message 3 for highlight`。
- highlight_start=7，highlight_end=18。

## 本轮修复

### 测试证据目录稳定化

- 修复文件：
  - `scripts\reliability_call_resume_suite.ps1`
  - `scripts\full_android_qa_suite.ps1`
- 变更：输出目录从 `tmp` 改到 `release-archives\qa-20260624`。
- 原因：`tmp` 目录会被清理，不适合作为正式测试证据目录。
- 验证：P0 和 P1 全量 QA 报告均已稳定写入 `release-archives\qa-20260624`。

### 系统面板覆盖误报修复

- 修复文件：`scripts\fast_real_device_full_suite.ps1`
- 问题：B 设备停留在 Android 通知/快捷设置面板，脚本截图不是 App 页面，导致消息列表/私聊/附件入口误判 FAIL。
- 修复：每次拉起 App 前后执行 `cmd statusbar collapse`。
- 验证：复测 `p1-full-flow-after-statusbar-fix`，16/16 通过。

## 剩余风险

- 厂商推送：模拟器缺 HMS/GMS，不能证明杀进程后的厂商推送真实可达；需要华为/小米/OPPO/VIVO/GMS 真机专项。
- 真实媒体通话：本轮验证了通话状态机和入口，真实麦克风、扬声器、摄像头、锁屏/后台持续通话还需要两台真机实测。
- 极限限流：历史 0ms burst 会触发 429；正常 1200ms 节流发送 40 条无丢失，但客户端仍应保留 429 明确提示和重发入口。

## 下一步建议

1. 用两台真机跑真实语音/视频媒体专项：拨打、接听、切后台、锁屏、外放/静音、挂断、再次拨打。
2. 跑厂商推送专项：杀进程通知到达、点击通知进入正确会话、消息不重复不丢失。
3. 跑网络切换专项：断网、弱网、恢复、后台恢复、WebSocket 重连和补拉消息。
4. 跑覆盖安装升级：旧包升级新包后登录态、本地消息、推送 token 是否保留。

## P1 继续推进记录 20:47-21:02

### 消息持久化专项

首次命令：

```powershell
& 'pwsh' -NoProfile -File .\scripts\fast_real_device_message_persistence.ps1 -BaseDir release-archives\qa-20260624\p1-message-persistence-rerun -DeviceB 127.0.0.1:16448
```

首次报告：

- `release-archives\qa-20260624\p1-message-persistence-rerun\report.md`
- `release-archives\qa-20260624\p1-message-persistence-rerun\message-persistence-final-logcat-scan-20260624-205052.json`

首次结果：

- total=3，pass=2，fail=1。
- 私聊消息持久化 PASS。
- 群聊消息持久化 PASS。
- 最终 logcat 扫描 FAIL，app_error_pattern_count=2。

定位：

- 两条 app_error 都是 MuMu 模拟器 `D/Watchdog` 启动/退出埋点。
- 日志内容为 `error: {code:0,msg:ok}`，不是 App 异常。
- 该失败属于测试脚本误判。

修复：

- 文件：`scripts\fast_real_device_message_persistence.ps1`
- 处理：logcat app error 扫描排除 MuMu Watchdog 的 `error code=0 msg=ok` 启动/退出事件。

复测命令：

```powershell
& 'pwsh' -NoProfile -File .\scripts\fast_real_device_message_persistence.ps1 -BaseDir release-archives\qa-20260624\p1-message-persistence-after-log-filter -DeviceB 127.0.0.1:16448
```

复测报告：

- `release-archives\qa-20260624\p1-message-persistence-after-log-filter\report.md`
- `release-archives\qa-20260624\p1-message-persistence-after-log-filter\latest-message-persistence-summary.json`

复测结果：

- total=3，pass=3，fail=0。
- fatal_pattern_count=0。
- app_error_pattern_count=0。
- 私聊测试消息：`persistpriv20260624-205203`
- 群聊测试消息：`persistgroup20260624-205203`
- 验证覆盖：发送后、返回列表、重新打开、切 tab、后台恢复、强杀重启后消息均仍可见。

### 快速压力操作

命令：

```powershell
& 'pwsh' -NoProfile -File .\scripts\fast_real_device_stress.ps1 -BaseDir release-archives\qa-20260624\p1-fast-stress-rerun -DeviceA 127.0.0.1:16416 -DeviceB 127.0.0.1:16448 -Iterations 18 -DelayMs 140
```

报告：

- `release-archives\qa-20260624\p1-fast-stress-rerun\report.md`
- `release-archives\qa-20260624\p1-fast-stress-rerun\latest-stress-summary.json`

结果：

- iterations=18。
- total=19，pass=19，fail=0。
- fatal_pattern_count=0。
- app_error_pattern_count=0。

### 增强 Monkey 360

命令：

```powershell
& 'pwsh' -NoProfile -File .\scripts\full_android_qa_suite.ps1 -SkipInstall -MonkeyEvents 360
```

报告：

- `release-archives\qa-20260624\android-full-qa-20260624-205916\report.md`
- `release-archives\qa-20260624\android-full-qa-20260624-205916\results.json`

结果：

- PASS=15，FAIL=0，WARN=3，INFO=5。
- 三台设备 Monkey 360 events 均未出现明确 crash/ANR marker。
- API 私聊消息、API 语音/视频、UI 消息持久化、UI 通话按钮均通过。
- WARN 仍为模拟器系统/HMS/GMS 类 logcat 噪声。

### 未读数压力 50 条

命令：

```powershell
& 'pwsh' -NoProfile -File .\scripts\stress_unread_push_consistency.ps1 -MessageCount 50 -SendDelayMs 200 -OutputPath release-archives\qa-20260624\unread-push-consistency-50.json
```

结果文件：

- `release-archives\qa-20260624\unread-push-consistency-50.json`

结果：

- message_count=50。
- expected_unread_after_send=50。
- device_a_unread_after_send=50。
- device_b_unread_after_send=50。
- device_a_unread_after_read=0。
- device_b_unread_after_read=0。
- push_tokens_bound=2。
- 过程中触发 429 限流重试 6 次，最终一致性通过。

## P1 继续推进结论

- 本轮 P1 继续测试未发现真实 App 功能失败。
- 发现并修复 1 个测试脚本误报：MuMu Watchdog `error code=0 msg=ok` 被误算为 app_error。
- 消息持久化、快速压力操作、增强 Monkey、未读数 50 条一致性均已通过。
- 429 限流在 50 条、200ms 间隔下仍会触发，但脚本重试后数据一致；建议客户端继续保留明确的限流提示和失败重发入口。

## P2 测试记录 21:04-21:32

### 长时 Monkey 1000

首次命令：

```powershell
& 'pwsh' -NoProfile -File .\scripts\full_android_qa_suite.ps1 -SkipInstall -MonkeyEvents 1000
```

首次报告：

- `release-archives\qa-20260624\android-full-qa-20260624-210422\report.md`

首次结果：

- PASS=13，FAIL=0，WARN=5，INFO=5。
- 三台设备 Monkey 1000 events 均未出现明确 crash/ANR marker。
- `127.0.0.1:16416` 出现 UI 消息持久化和通话按钮 WARN。

定位：

- `16416` 截图/XML 显示当前被 Android 系统层覆盖。
- 具体包括“屏幕投射”系统弹窗和快捷设置/通知面板。
- 不是消息消失，也不是通话按钮缺失。

修复：

- 文件：`scripts\full_android_qa_suite.ps1`
- 新增：`Dismiss-SystemUi`
- 处理：UI 检查前执行 `cmd statusbar collapse`；如果仍是 `com.android.systemui`，再按 `KEYCODE_BACK` 清理系统弹窗。
- 调用点：登录检查、消息持久化检查、通话入口检查、快速导航前。

复测命令：

```powershell
& 'pwsh' -NoProfile -File .\scripts\full_android_qa_suite.ps1 -SkipInstall -MonkeyEvents 1000
```

复测报告：

- `release-archives\qa-20260624\android-full-qa-20260624-210749\report.md`
- `release-archives\qa-20260624\android-full-qa-20260624-210749\results.json`

复测结果：

- PASS=15，FAIL=0，WARN=3，INFO=5。
- UI 消息持久化两台主测设备均 PASS。
- UI 通话按钮两台主测设备均 PASS。
- 三台设备 Monkey 1000 events 均 PASS。
- 剩余 WARN 为模拟器系统/HMS/GMS/logcat 噪声。

### 极限消息/限流 100 条

命令：

```powershell
& 'pwsh' -NoProfile -File .\scripts\stress_unread_push_consistency.ps1 -MessageCount 100 -SendDelayMs 100 -OutputPath release-archives\qa-20260624\unread-push-consistency-100.json
```

结果文件：

- `release-archives\qa-20260624\unread-push-consistency-100.json`

结果：

- message_count=100。
- send_delay_ms=100。
- expected_unread_after_send=100。
- device_a_unread_after_send=100。
- device_b_unread_after_send=100。
- device_a_unread_after_read=0。
- device_b_unread_after_read=0。
- push_tokens_bound=2。
- 过程中多次触发 429 限流重试，最终一致性通过。

结论：

- P2 极限发送下不会丢未读一致性。
- 429 在高频发送下仍然明显，应继续保留客户端限流提示和重发入口。

### 网络切换/恢复专项

新增脚本：

- `scripts\p2_network_resume_smoke.ps1`

目标：

- Bob 设备断网。
- Alice 通过 API 发消息。
- 恢复 Bob 网络。
- 拉起 Bob App，验证消息能补到 UI。

执行记录：

- `release-archives\qa-20260624\p2-network-resume\report.md`
- `release-archives\qa-20260624\p2-network-resume-after-restore-fix\report.md`
- `release-archives\qa-20260624\p2-network-resume-after-double-restore\report.md`

结果：

- 断网可确认：`network_off_confirmed=true`。
- API 消息存在：`api_found=true`。
- MuMu 上 airplane/network 恢复在脚本内无法稳定恢复，`network_restored=false`。
- `127.0.0.1:16448` 后续进入 ADB offline 状态。
- `127.0.0.1:16384` 和 `127.0.0.1:16416` 仍正常，后端访问正常。

处理：

- 已手动尝试 `settings put global airplane_mode_on 0`、`cmd connectivity airplane-mode disable`、`svc wifi enable`、`svc data enable`、ADB reconnect、ADB server restart。
- `16448` 仍显示 `emulator-5558 offline`。
- 为避免扩大影响，未继续对其他设备执行 airplane 网络切换。

判定：

- 当前网络切换专项为 MuMu/ADB 环境阻塞，不能作为 App 消息恢复失败结论。
- 后续建议用真机或支持稳定网络控制的模拟器做断网/恢复专项。

### 覆盖安装升级

构建命令：

```powershell
& 'pwsh' -NoProfile -File .\scripts\build-local-emulator-apk.ps1
```

构建结果：

- 首次 release 构建遇到 `GeneratedPluginRegistrant.java` 引用 `integration_test`。
- 构建脚本已移除 test-only registrant 后产出 APK。
- APK：`artifacts\local-emulator-apk-20260624-213015\genericim-local-emulator-release.apk`

安装命令：

```powershell
& 'pwsh' -NoProfile -File .\scripts\install-local-emulator-apk.ps1 -ApkPath artifacts\local-emulator-apk-20260624-213015\genericim-local-emulator-release.apk -DeviceId 127.0.0.1:16416
```

安装结果：

- `Performing Streamed Install`
- `Success`

验证报告：

- `release-archives\qa-20260624\p2-upgrade-install-16416\report.md`
- `release-archives\qa-20260624\p2-upgrade-install-16416\summary.json`
- `release-archives\qa-20260624\p2-upgrade-install-16416\after-upgrade.xml`
- `release-archives\qa-20260624\p2-upgrade-install-16416\after-upgrade.png`

验证结果：

- status=PASS。
- 覆盖安装后 App 可启动。
- UI 仍为已登录/主界面或聊天态。
- 未回到登录页。

## P2 当前结论

- 长时 Monkey 1000：通过，未发现 crash/ANR。
- 极限消息 100 条：通过，未读一致性和已读回落正常。
- 覆盖安装升级：通过，`16416` 覆盖安装后登录态/主界面状态保留。
- 网络切换：MuMu/ADB 环境阻塞，`16448` 被 airplane/network 切换打到 offline；不能证明 App 有消息恢复缺陷，需真机或更稳定模拟器复测。
- 本轮真实 App 问题未发现；修复的是测试脚本对系统层覆盖的误判，以及新增网络专项脚本后发现 MuMu 网络控制不稳定。

## 真机 P0 专项测试记录 - 2026-06-24 23:07

范围：

- 设备 A：`8MY0220C17006781`，Huawei `ELS_AN00`。
- 设备 B：`UQG5T20915006269`，Huawei `ELS_AN00`。
- 包名：`com.genericim.ma100`。
- APK：`artifacts\local-real-device-apk-20260624-224822\genericim-local-real-device-release.apk`。
- 后端：`http://192.168.1.100:8080`，本机 health 通过。
- 测试账号：`smoke_alice` / `smoke_bob`。

本轮先发现并修复的测试脚本问题：

- `scripts\reliability_call_resume_suite.ps1` 默认用模拟器地址/`curl` 做设备端健康检查；华为真机没有内置 `curl`，导致误报网络失败。已改为有 `curl` 时请求 `/health`，无 `curl` 时用 `ping` 验证局域网主机可达。
- 旧脚本只通过 API 创建测试账号和会话，没有把两台真机 App 登录到对应账号，导致后台恢复检查停留在登录页或桌面，不能作为 App bug。已补真机 UI 登录前置。
- 首次补登录时键盘遮挡登录按钮和协议区域，脚本没有真正提交登录。已改为填完密码后先收起键盘，再重新 dump UI，勾选协议并点击登录。
- PowerShell 函数内 `$matches` 与自动变量冲突，导致 `Find-Nodes` 报错。已改为 `$foundNodes`。

正式 P0 命令：

```powershell
& 'pwsh' -NoProfile -File .\scripts\reliability_call_resume_suite.ps1 `
  -AliceDevice 8MY0220C17006781 `
  -BobDevice UQG5T20915006269 `
  -EmulatorBaseUrl http://192.168.1.100:8080 `
  -BurstCount 20 `
  -SendDelayMs 1200 `
  -Api429RetryCount 1 `
  -CallCycles 3
```

正式报告：

- `release-archives\qa-20260624\reliability-call-resume-20260624-230726\report.md`
- `release-archives\qa-20260624\reliability-call-resume-20260624-230726\results.json`
- `release-archives\qa-20260624\reliability-call-resume-20260624-230726\message-reliability.json`
- `release-archives\qa-20260624\reliability-call-resume-20260624-230726\background-resume.json`
- `release-archives\qa-20260624\reliability-call-resume-20260624-230726\8MY0220C17006781-logcat.txt`
- `release-archives\qa-20260624\reliability-call-resume-20260624-230726\UQG5T20915006269-logcat.txt`

正式结果：

- PASS=9，FAIL=0，WARN=2。
- 后端健康：PASS。
- 两台真机局域网可达：PASS；华为系统无 `curl`，通过 `ping 192.168.1.100` 验证可达。
- 两台真机登录测试账号：PASS。
- 消息可靠性：PASS；每边 20 条，总计 40 条，`send_errors=[]`，`missing_for_alice=[]`，`missing_for_bob=[]`，耗时 `49761ms`。
- 通话二次拉起：PASS；3 轮 `create/reject/cancel/video accept/end` 均成功。
- 后台和强杀恢复：PASS；HOME 后恢复、force-stop 后重启，两台设备最新消息均可见。

logcat 复核：

- 报告中 `WARN` 来自宽泛关键字匹配。
- 二次筛查 `FATAL EXCEPTION|ANR in com.genericim.ma100|FlutterError|E/flutter|Input dispatching timed out|message.*failed|call.*failed|WebSocket.*(error|failed)` 未命中。
- 当前 WARN 主要是 HMS/AGC、系统、`uiautomator` 采集噪声，不作为 App P0 bug。

P0 结论：

- 当前真机 P0 未发现 App 业务 bug。
- 用户最关注的消息突然消失/丢失：本轮 40 条突发消息和后台/强杀恢复均未复现。
- 通话二次拉起：3 轮通过，未复现二次无法拉起。
- 本轮实际修复为测试脚本修复，保证后续真机 P0/P1/P2 结论不再被模拟器地址、无 `curl`、未登录、键盘遮挡等前置问题污染。

## 真机 P1 主功能测试记录 - 2026-06-24 23:22

范围：

- 设备 A：`8MY0220C17006781`，Huawei `ELS_AN00`。
- 设备 B：`UQG5T20915006269`，Huawei `ELS_AN00`。
- 包名：`com.genericim.ma100`。
- P0 前置：已通过 `release-archives\qa-20260624\reliability-call-resume-20260624-230726\report.md`。
- P1 原则：主功能快速覆盖，发现主路径 FAIL 先修复并回归；高风险写入只做低风险文本消息，不提交支付/提现/充值/红包/转账/删除/转发/文件选择。

### P1-1 全量 UI 快速流程

命令：

```powershell
& 'pwsh' -NoProfile -File .\scripts\fast_real_device_full_suite.ps1 `
  -BaseDir release-archives\qa-20260624\real-device-p0-p3\p1-full-flow `
  -DeviceA 8MY0220C17006781 `
  -DeviceB UQG5T20915006269
```

结果：

- 总计 `16` 项，PASS=`16`，FAIL=`0`，tool_warn=`0`。
- final logcat：`fatal_pattern_count=0`，`app_error_pattern_count=0`。
- 报告：`release-archives\qa-20260624\real-device-p0-p3\p1-full-flow\report.md`。
- 汇总：`release-archives\qa-20260624\real-device-p0-p3\p1-full-flow\latest-full-suite-summary.json`。

覆盖点：

- A/B 设备预检和截图采集。
- 设置首页和二级入口。
- 钱包首页、充值页只读检查、提现 0 余额填充、账单、关于钱包、支付密码入口。
- 消息列表、私聊详情头部和输入栏。
- 私聊附件面板。
- 私聊长按菜单打开，不确认任何菜单动作。
- 群聊头部和附件面板。
- 联系人、发现、设置 Tab。
- 通话按钮可见性检查，不发起通话。

### P1-3 消息持久化专项

命令：

```powershell
& 'pwsh' -NoProfile -File .\scripts\fast_real_device_message_persistence.ps1 `
  -BaseDir release-archives\qa-20260624\real-device-p0-p3\p1-message-persistence `
  -DeviceB UQG5T20915006269
```

结果：

- 总计 `3` 项，PASS=`3`，FAIL=`0`，tool_warn=`0`。
- 私聊测试消息：`persistpriv20260624-231640`。
- 群聊测试消息：`persistgroup20260624-231640`。
- final logcat：`fatal_pattern_count=0`，`app_error_pattern_count=0`。
- 报告：`release-archives\qa-20260624\real-device-p0-p3\p1-message-persistence\report.md`。
- 汇总：`release-archives\qa-20260624\real-device-p0-p3\p1-message-persistence\latest-message-persistence-summary.json`。

覆盖点：

- 私聊消息发送后可见。
- 私聊消息在重进会话、Tab 切换、后台恢复、强杀重启后仍可见。
- 群聊消息发送后可见。
- 群聊消息在重进会话、Tab 切换、后台恢复、强杀重启后仍可见。

### P1 低风险写入流

命令：

```powershell
& 'pwsh' -NoProfile -File .\scripts\fast_real_device_write_flows.ps1 `
  -BaseDir release-archives\qa-20260624\real-device-p0-p3\p1-write-flows `
  -DeviceB UQG5T20915006269
```

结果：

- 总计 `3` 项，PASS=`3`，FAIL=`0`，tool_warn=`0`。
- 私聊真实发送文本：`codexpriv20260624-232144`。
- 群聊真实发送文本：`codexgroup20260624-232144`。
- final logcat：`fatal_pattern_count=0`，`app_error_pattern_count=0`。
- 报告：`release-archives\qa-20260624\real-device-p0-p3\p1-write-flows\report.md`。
- 汇总：`release-archives\qa-20260624\real-device-p0-p3\p1-write-flows\latest-write-flows-summary.json`。

覆盖点：

- 私聊文本消息真实发送并在 UI 可见。
- 群聊文本消息真实发送并在 UI 可见。
- 未执行支付、提现、充值、红包、转账、删除、转发、文件上传/选择等高风险提交动作。

P1 结论：

- 当前真机 P1 未发现 App 业务 bug。
- 主功能入口、设置、钱包只读路径、消息列表、私聊、群聊、附件面板、长按菜单、Tab 切换、通话按钮可见性均通过。
- 私聊和群聊消息持久化通过，未复现消息突然消失。
- 三组 P1 脚本最终 logcat 均为 `fatal=0` / `app_error=0`。
- 未覆盖或仅只读覆盖：图片/视频/文件选择、红包、转账、充值提交、提现提交、删除/转发确认、真实音视频接听操作。这些应放到 P2/P3 或专门人工授权场景，避免破坏账号资产和本地数据。

## 真机 P2 可靠性和压力测试记录 - 2026-06-24 23:59

范围：

- 设备 A：`8MY0220C17006781`，Huawei `ELS_AN00`。
- 设备 B：`UQG5T20915006269`，Huawei `ELS_AN00`。
- 包名：`com.genericim.ma100`。
- APK：`artifacts\local-real-device-apk-20260624-224822\genericim-local-real-device-release.apk`。
- P0 前置：`release-archives\qa-20260624\reliability-call-resume-20260624-230726\report.md` 已通过。
- P1 前置：`p1-full-flow`、`p1-message-persistence`、`p1-write-flows` 已通过。
- P2 原则：做真机快速压力、随机点击、消息未读一致性、断网恢复、覆盖安装、后台推送；仍不做支付/提现/充值/红包/转账/删除确认等破坏性动作。

### P2-1 快速压力操作

命令：

```powershell
& 'pwsh' -NoProfile -File .\scripts\fast_real_device_stress.ps1 `
  -BaseDir release-archives\qa-20260624\real-device-p0-p3\p2-fast-stress `
  -DeviceA 8MY0220C17006781 `
  -DeviceB UQG5T20915006269 `
  -Iterations 30 `
  -DelayMs 120
```

结果：

- 总计 `31` 项，PASS=`31`，FAIL=`0`，tool_warn=`0`。
- final logcat：`fatal=0`，`app_error=0`。
- 报告：`release-archives\qa-20260624\real-device-p0-p3\p2-fast-stress\report.md`。
- 结果 JSON：`release-archives\qa-20260624\real-device-p0-p3\p2-fast-stress\stress-results-20260624-233301.json`。
- final logcat 扫描：`release-archives\qa-20260624\real-device-p0-p3\p2-fast-stress\stress-final-logcat-scan-20260624-233301.json`。

覆盖点：

- 快速随机打开消息、联系人、发现、设置、钱包、会话、群聊、附件入口、长按菜单等低风险路径。
- 未发送二次附件、未提交支付/提现/充值/红包/转账、未确认删除/转发。

### P2-2 Monkey 随机点击

修复记录：

- 首轮 `full_android_qa_suite.ps1` 调用方式不适合真机 Monkey：`-Devices a,b` 被当作单个字符串，`-SkipUi` 会跳过 Monkey，设备连通检查也带有模拟器/curl 假设。
- 新增真机专用脚本：`scripts\p2_real_device_monkey.ps1`。
- 脚本中曾使用 `$Args` 参数名，和 PowerShell 自动变量冲突，导致 adb 打印 help、Monkey 未真正执行；已改为 `$AdbArgs` 后重跑。

最终结果：

- 两台真机各 `1000` 次 Monkey 事件。
- 总计 PASS=`2`，FAIL=`0`。
- 两台设备 `monkey_exit=0`，`fatal=0`，焦点仍在 `com.genericim.ma100`。
- 报告：`release-archives\qa-20260624\real-device-p0-p3\p2-monkey\report.md`。
- JSON：`release-archives\qa-20260624\real-device-p0-p3\p2-monkey\p2-real-device-monkey-20260624-233833.json`。
- 设备日志：
  - `release-archives\qa-20260624\real-device-p0-p3\p2-monkey\8MY0220C17006781-monkey-20260624-233833.txt`。
  - `release-archives\qa-20260624\real-device-p0-p3\p2-monkey\UQG5T20915006269-monkey-20260624-233833.txt`。

结论：

- Monkey 最终通过。
- 本项发现并修复的是测试脚本问题，不是 App 业务 bug。

### P2-3 未读数和消息一致性压力

脚本：

```powershell
.\scripts\stress_unread_push_consistency.ps1
```

20 条消息结果：

- `message_count=20`。
- `expected_unread_after_send=20`。
- `device_a_unread_after_send=20`。
- `device_b_unread_after_send=20`。
- 已读后两台设备均清零：`device_a_unread_after_read=0`，`device_b_unread_after_read=0`。
- 推送 token 绑定数：`push_tokens_bound=2`。
- JSON：`release-archives\qa-20260624\real-device-p0-p3\p2-unread-consistency\unread-20.json`。

100 条消息结果：

- `message_count=100`。
- `expected_unread_after_send=100`。
- `device_a_unread_after_send=100`。
- `device_b_unread_after_send=100`。
- 已读后两台设备均清零：`device_a_unread_after_read=0`，`device_b_unread_after_read=0`。
- 推送 token 绑定数：`push_tokens_bound=2`。
- JSON：`release-archives\qa-20260624\real-device-p0-p3\p2-unread-consistency\unread-100.json`。

风险说明：

- 100 条高频发送中触发过服务端 `429` 限流，脚本按退避重试后全部完成。
- 最终 API、未读数、两台真机 UI 一致，未发现消息丢失或未读数错乱。

### P2-4 真机断网恢复

修复记录：

- 旧脚本 `scripts\p2_network_resume_smoke.ps1` 带有模拟器地址、airplane mode、curl 假设，不适合作为真机结论依据。
- 新增安全真机脚本：`scripts\p2_real_device_network_resume.ps1`。
- 脚本只在 Bob 设备上执行 `svc wifi disable` / `svc data disable`，通过 `finally` 保证恢复网络，并用 `ping 192.168.1.100` 做真机连通确认。

结果：

- 状态：PASS。
- 设备：`UQG5T20915006269`。
- 测试消息：`p2-real-network-20260624-234746`。
- `initial_reachable=True`。
- `network_off_confirmed=True`。
- `network_restored=True`。
- `api_found=True`。
- `ui_found=True`。
- 报告：`release-archives\qa-20260624\real-device-p0-p3\p2-network-resume-real\report.md`。
- JSON：`release-archives\qa-20260624\real-device-p0-p3\p2-network-resume-real\network-resume-20260624-234746.json`。

结论：

- 真机断网期间由另一端/API 写入的消息，恢复网络后 API 和 UI 均可查到。
- 未复现恢复后消息消失、消息列表不刷新、断网后无法恢复连接。

### P2-5 覆盖安装和升级后回归

安装结果：

- 使用 APK：`artifacts\local-real-device-apk-20260624-224822\genericim-local-real-device-release.apk`。
- 两台真机执行 `adb install -r`，结果均为 `Success`。
- 证据目录：`release-archives\qa-20260624\real-device-p0-p3\p2-upgrade-install-real`。
- 证据文件：
  - `8MY0220C17006781-after-upgrade.png` / `.xml` / `window.txt`。
  - `UQG5T20915006269-after-upgrade.png` / `.xml` / `window.txt`。

升级后状态：

- 两台设备仍保持登录状态。
- 均停留在消息列表或主界面，不是登录页。
- 断网恢复专项消息仍可在列表中看到。

升级后 P0 快速回归：

- 报告：`release-archives\qa-20260624\reliability-call-resume-20260624-235009\report.md`。
- PASS=`9`，FAIL=`0`，WARN=`2`。
- 消息可靠性：每边 2 条，总计 4 条，无缺失。
- 通话二次拉起：1 轮 `create/reject/cancel/video accept/end` 通过。
- 后台和强杀恢复：两台设备最新消息均可见。
- WARN 来自宽泛 logcat 关键字，二次筛查 `FATAL EXCEPTION|ANR in com.genericim.ma100|FlutterError|E/flutter|Input dispatching timed out|message.*failed|call.*failed|WebSocket.*(error|failed)` 未命中。

结论：

- 覆盖安装未导致退出登录、消息丢失、启动失败或通话二次拉起回归。

### P2-6 后台推送通知

脚本：

```powershell
.\scripts\p2_real_device_push_notification.ps1
```

结果：

- 状态：PASS。
- 设备：`UQG5T20915006269`。
- 测试消息：`p2-push-20260624-235431`。
- `notification_package_seen=True`。
- `notification_text_seen=True`。
- `api_found=True`。
- `ui_found=True`。
- 报告：`release-archives\qa-20260624\real-device-p0-p3\p2-push-notification-real\report.md`。
- JSON：`release-archives\qa-20260624\real-device-p0-p3\p2-push-notification-real\push-notification-20260624-235431.json`。
- 通知 dump：`release-archives\qa-20260624\real-device-p0-p3\p2-push-notification-real\notification-20260624-235431.txt`。
- logcat：`release-archives\qa-20260624\real-device-p0-p3\p2-push-notification-real\logcat-20260624-235431.txt`。

结论：

- Bob 退到后台后，Alice/API 发送消息，系统通知中能看到 `com.genericim.ma100` 包和测试消息文本。
- 打开 App 后，API 和 UI 均能查到同一条消息。

P2 结论：

- 当前真机 P2 未发现 App 业务 bug。
- 快速压力、Monkey、未读一致性、断网恢复、覆盖安装、升级后 P0 回归、后台推送通知均通过。
- 用户重点关注的“消息记录突然消失”：P2 的 20 条、100 条未读一致性、断网恢复、覆盖安装后回归均未复现。
- 用户重点关注的“通话二次拉起”：升级后 P0 快速回归 1 轮通过；此前 P0 3 轮通过。
- 本轮实际修复的是测试脚本和真机适配问题，包括 Monkey 真机脚本、断网恢复真机脚本、后台推送真机脚本；没有发现需要改 App 业务代码的 P2 问题。

P2 未覆盖和后续建议：

- 未做 10 分钟以上真实音视频通话稳定性测试；建议放到 P3 或专项人工长通话测试。
- 未做锁屏状态下的完整推送点击链路；本轮覆盖的是 HOME 后台状态通知。
- 未提交支付、充值、提现、红包、转账、删除确认、文件选择/上传等会改变资产或本地数据的破坏性动作。
- 服务端高频消息会触发 `429` 限流；当前脚本退避重试后业务一致性正常。若后续要做更高并发压测，应单独评估限流阈值和产品提示。

## 真机 P3 兼容性和发布前体验测试记录 - 2026-06-25 00:13

范围：

- 设备 A：`8MY0220C17006781`，Huawei `ELS_AN00`，Android 12，构建 `ELS-AN00 4.2.0.121(C00E121R6P3)`。
- 设备 B：`UQG5T20915006269`，Huawei `ELS_AN00`，Android 12，构建 `ELS-AN00 4.2.0.120(C00E100R6P3)`。
- 包名：`com.genericim.ma100`。
- APK：`artifacts\local-real-device-apk-20260624-224822\genericim-local-real-device-release.apk`。
- P3 原则：覆盖发布前体验、权限、系统显示、性能快照、发布包元数据和签名；不执行退出登录、拉黑、群成员变更、支付密码、红包、转账、充值、提现、删除确认等会改变账号状态或资产的数据破坏动作。

新增脚本：

- `scripts\p3_real_device_release_experience.ps1`。
- 脚本会保存截图、UI XML、logcat、meminfo、冷启动、APK badging、APK 签名证据。
- 脚本在 `finally` 中恢复深色模式、字体比例、自动旋转和屏幕方向，避免测试后真机系统状态被污染。
- 首轮脚本曾把“横屏截图采集成功”直接记为 PASS；抽查发现华为设备 `SurfaceOrientation=0`，并未真正横屏。已修复脚本：强制旋转后读取 `SurfaceOrientation`，未实际横屏时记为 `WARN`，不再误报为完整横屏覆盖。

命令：

```powershell
& 'pwsh' -NoProfile -File .\scripts\p3_real_device_release_experience.ps1
```

最终结果：

- 状态：`WARN`。
- PASS=`22`，WARN=`2`，FAIL=`0`。
- 报告：`release-archives\qa-20260624\real-device-p0-p3\p3-release-experience\report.md`。
- JSON：`release-archives\qa-20260624\real-device-p0-p3\p3-release-experience\p3-release-experience-20260625-001110.json`。

### P3-1 权限和系统兼容

结果：

- 两台真机均采集设备信息、包权限状态、权限撤销后重启截图、权限恢复后状态。
- 相机权限撤销/恢复：PASS。
- 麦克风权限撤销/恢复：PASS。
- 权限原始状态：两台设备 `camera=True`，`microphone=True`。
- 证据：
  - `release-archives\qa-20260624\real-device-p0-p3\p3-release-experience\8MY0220C17006781\package-permissions-20260625-001110.txt`。
  - `release-archives\qa-20260624\real-device-p0-p3\p3-release-experience\UQG5T20915006269\package-permissions-20260625-001110.txt`。

说明：

- 本轮自动化覆盖了相机和麦克风权限的拒绝/恢复基础稳定性。
- 相册、定位、通知、悬浮窗/后台弹出、通讯录的深度授权文案和业务入口联动未做人工逐项确认，应在发布前人工补充。

### P3-2 多品牌真机

结果：

- 华为/Honor 类 HMS 设备：两台华为 `ELS_AN00` 已覆盖。
- 两台设备 P0/P1/P2/P3 均未出现 App 级崩溃或 ANR。
- 小米、OPPO/VIVO、Google/GMS、低端机：本轮没有对应真机，未覆盖。

结论：

- 当前只能证明华为 Android 12 设备发布前体验通过基础检查。
- 厂商差异类问题仍建议后续补小米、OPPO/VIVO、GMS 设备。

### P3-3 UI 和体验巡检

覆盖：

- 正常竖屏截图：PASS。
- 深色模式截图：PASS。
- 1.3 倍字体截图：PASS。
- VIP/SVIP 列表展示抽查：PASS，未看到明显文字重叠或标签溢出。
- 聊天页消息记录抽查：PASS，消息和输入栏可见，未看到空白页。

证据目录：

- `release-archives\qa-20260624\real-device-p0-p3\p3-release-experience\8MY0220C17006781`。
- `release-archives\qa-20260624\real-device-p0-p3\p3-release-experience\UQG5T20915006269`。

WARN：

- 两台华为设备强制横屏后 `SurfaceOrientation=0`，系统未实际进入横屏。
- 因此横屏 UI 只保存了当前竖屏证据，不算完整横屏适配覆盖。
- 这不是 App 崩溃或业务 FAIL，但属于 P3 覆盖限制。若产品要求支持横屏，需要再用能实际旋转的设备或明确锁定竖屏策略。

### P3-4 性能和资源

冷启动：

- `8MY0220C17006781`：`TotalTime=284ms`，`WaitTime=287ms`。
- `UQG5T20915006269`：`TotalTime=300ms`，`WaitTime=305ms`。

内存快照：

- `8MY0220C17006781`：冷启动后 `TOTAL PSS=417216KB`。
- `UQG5T20915006269`：冷启动后 `TOTAL PSS=291590KB`。

logcat 严重错误扫描：

- 两台设备均未命中 `FATAL EXCEPTION|ANR in com.genericim.ma100|FlutterError|E/flutter|RenderFlex|Input dispatching timed out|message.*failed|call.*failed|WebSocket.*(error|failed)`。

未覆盖：

- 1000 条消息滚动专项。
- 图片列表滚动专项。
- 视频播放专项。
- 10 分钟以上真实音视频通话 CPU/发热。
- 后台 30 分钟耗电和内存泄漏趋势。

### P3-5 安全和隐私

本轮未执行：

- 退出登录后旧账号数据检查。
- 切换账号后聊天列表不串号。
- 支付密码页面输入和遮罩。
- token 过期后重新登录。
- 拉黑后骚扰拦截。
- 群成员保护变更。

原因：

- 这些动作会改变账号状态、登录态、关系链或敏感业务数据，不适合在未授权的自动化脚本中直接执行。
- 建议作为 P3 人工授权专项或使用独立测试账号/重置数据库环境执行。

### P3-6 发布包检查

APK badging：

- 包名：`com.genericim.ma100`，PASS。
- 应用名：`通用IM`，PASS。
- `versionCode=14`。
- `versionName=4.0.3`。
- `debuggable=False`。
- 证据：`release-archives\qa-20260624\real-device-p0-p3\p3-release-experience\apk-badging-20260625-001110.txt`。

APK 签名：

- SHA256：`1C730B89510FCB25F9DEE2DE2E9E6C792391AB755536A2150346EF5E0E22D01F`。
- 与备案/指定证书 SHA256 一致，PASS。
- SHA1：`912193cd065e583dd1d9add441c373bff44f1099`。
- MD5：`a8bf6280db4975bcc5fc302922fe7c24`。
- 证据：`release-archives\qa-20260624\real-device-p0-p3\p3-release-experience\apk-signature-20260625-001110.txt`。

P3 结论：

- 当前真机 P3 未发现 App 业务 FAIL。
- 发布包包名、应用名、版本、debuggable、签名证书均通过检查。
- 两台华为真机的正常竖屏、深色模式、大字体、权限撤销/恢复、冷启动、内存快照、严重 logcat 扫描均通过。
- 当前仅有 `2` 个 WARN：两台华为设备强制横屏未实际触发，不能作为完整横屏适配通过证据。
- 本轮实际修复的是 P3 测试脚本判定逻辑，避免把未实际横屏误报为 PASS；没有发现需要修复 App 业务代码的问题。

P3 后续建议：

- 如果产品只支持竖屏，建议在发布说明或代码策略里明确竖屏锁定，P3 横屏项可改为“不适用”。
- 如果产品要求横屏，需在能实际旋转的真机上补横屏专项，并检查聊天页、通话页、钱包页、资料页。
- 发布前仍建议补人工授权专项：锁屏通知点击进入会话、10 分钟真实语音/视频通话、退出登录/切换账号隐私、支付密码遮罩、拉黑和群成员保护。
