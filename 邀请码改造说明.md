# 邀请码系统改造说明

## 改造概述

将原有的独立邀请码系统改为使用用户手机号作为邀请码。这样每个用户（特别是官方客服）自动拥有唯一的邀请码，无需单独创建和管理邀请码。

## 改动文件清单

### 后端文件

1. **backend/internal/handlers/invite_code_handler.go**
   - `ValidateInviteCode`: 修改为支持通过手机号查找邀请码
   - `ProcessInviteCode`: 简化逻辑，直接使用手机号作为邀请码

2. **backend/internal/handlers/service_admin_handler.go**
   - `GetProfile`: 使用用户手机号作为邀请码
   - `GetDashboard`: 修改统计逻辑，通过联系人关系查询邀请用户
   - `GetInviteCode`: 返回用户手机号作为邀请码
   - `GetInvitees`: 通过联系人关系查询被邀请用户

3. **backend/internal/handlers/setting_handler.go**
   - `GetOfficialUsers`: 使用用户手机号作为邀请码
   - `GetOfficialUserInvitees`: 通过联系人关系查询被邀请用户

## 工作原理

### 邀请码格式
- 邀请码现在使用用户手机号，如 `13800138000`

### 邀请码验证流程
1. 用户注册时输入邀请码（手机号）
2. 系统通过手机号查找对应的官方客服
3. 验证找到的用户是否是已启用的官方客服
4. 如果验证通过，自动添加该官方客服为联系人并创建私聊

### 邀请统计
- 通过 `contacts` 表的创建关系来统计邀请用户
- 官方客服的仪表板显示通过邀请码注册的用户列表

## 优点

1. **简化系统**：无需维护独立的邀请码表（虽然表仍然存在，但不再作为主要邀请码来源）
2. **唯一性**：手机号天然唯一，不会冲突
3. **易用性**：官方客服自动拥有邀请码（其手机号），无需手动创建
4. **易记性**：手机号比UUID更容易记忆和分享
5. **兼容性**：仍支持旧的邀请码格式（如果invite_codes表中有数据）

## 注意事项

1. 旧的邀请码数据仍然存在于 `invite_codes` 表中，但新系统不再依赖它
2. 官方客服必须绑定手机号才能使用邀请功能
3. 邀请注册链接格式变更：
   - 旧格式：`/register?invite_code=abc123`
   - 新格式：`/register?invite_code=13800138000`（用户手机号）

## 前端改动

前端代码无需修改，因为API接口保持不变，只是返回的邀请码值从UUID变为手机号。

## 测试建议

1. 创建新的官方客服账号并绑定手机号，验证其邀请码是否为手机号
2. 使用新邀请码（手机号）注册新用户，验证是否成功
3. 检查官方客服仪表板的邀请统计是否正确
4. 验证旧的邀请码（如果存在）是否仍然有效

## 修改历史

- 2026-07-28: 将邀请码从UUID改为手机号