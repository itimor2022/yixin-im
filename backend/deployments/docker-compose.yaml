version: '3.8'

services:
  # Go后端服务
  api:
    build:
      context: ..
      dockerfile: deployments/Dockerfile
    ports:
      - "8080:8080"
    environment:
      - GIN_MODE=release
    volumes:
      - ../uploads:/app/uploads
    depends_on:
      - mysql
      - mongodb
      - redis
    networks:
      - grimimim
    restart: unless-stopped

  # MySQL数据库
  mysql:
    image: mysql:8.0
    container_name: grimimim-mysql
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: grimimim
      MYSQL_DATABASE: grimimim
      MYSQL_USER: grimimim
      MYSQL_PASSWORD: grimimim
    volumes:
      - mysql_data:/var/lib/mysql
      - ../scripts/init.sql:/docker-entrypoint-initdb.d/init.sql
    command: --default-authentication-plugin=mysql_native_password --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    networks:
      - grimimim
    restart: unless-stopped

  # MongoDB数据库
  mongodb:
    image: mongo:7.0
    container_name: grimimim-mongodb
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: grimimim
      MONGO_INITDB_ROOT_PASSWORD: grimimim
      MONGO_INITDB_DATABASE: grimimim_messages
    volumes:
      - mongodb_data:/data/db
    networks:
      - grimimim
    restart: unless-stopped

  # Redis缓存
  redis:
    image: redis:7-alpine
    container_name: grimimim-redis
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes --maxmemory 512mb --maxmemory-policy allkeys-lru
    volumes:
      - redis_data:/data
    networks:
      - grimimim
    restart: unless-stopped

  # Redis Commander (可视化管理)
  redis-commander:
    image: rediscommander/redis-commander:latest
    container_name: grimimim-redis-commander
    ports:
      - "8081:8081"
    environment:
      - REDIS_HOSTS=local:redis:6379
    depends_on:
      - redis
    networks:
      - grimimim
    restart: unless-stopped

networks:
  grimimim:
    driver: bridge

volumes:
  mysql_data:
  mongodb_data:
  redis_data:
